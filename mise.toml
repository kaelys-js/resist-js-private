# @generated by mirroring packages/shared/utils/cli/src/tools/sync/template/packages/mise.toml.hbs
# DO NOT EDIT directly — update the .hbs template and re-render.
# https://mise.jdx.dev/configuration.html

min_version = "2026.5.0"

[settings]
# Read .nvmrc for Node version
idiomatic_version_file_enable_tools = ["node"]
# Enable Corepack for pnpm (managed via packageManager field in package.json)
node.corepack = true

[env]
# Workspace-scoped tool installations — all binaries live inside the repo
MISE_INSTALLS_DIR = "{{config_root}}/.mise/installs"

[tools]
# System tools (none pinned at this layer; node from .nvmrc, pnpm from packageManager)
