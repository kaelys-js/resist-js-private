/**
 * Lens manifest for the ThermostatControl component (iot
 * category) — temperature dial control. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'iot',
  tags: ['thermostat', 'temperature', 'iot'],
  description: 'Temperature dial control.',
};
