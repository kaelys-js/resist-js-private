import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['schema', 'viewer', 'structure', 'explorer'],
  description: 'SchemaViewer component — awaiting implementation.',
};
