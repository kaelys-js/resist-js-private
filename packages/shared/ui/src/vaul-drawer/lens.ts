/**
 * Lens manifest for the VaulDrawer component (overlay
 * category) — mobile pull-to-dismiss drawer. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['drawer', 'vaul', 'mobile', 'gesture'],
  description:
    'A drawer component specifically designed for mobile with touch-based pull-to-dismiss interaction, inspired by native mobile sheets.',
};
