/**
 * Lens manifest for the StreamGraph component (data-display
 * category) — stacked-area theme-river chart. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['stream', 'stacked', 'chart'],
  description: 'Stacked area flow chart.',
};
