import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['product', 'quick-view', 'modal'],
  description: 'Quick preview product modal.',
};
