import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['countdown', 'timer', 'time', 'animated'],
  description: 'An animated countdown display showing remaining time or cycling through numbers.',
};
