/**
 * Lens manifest for the Countdown component (data-display
 * category) — animated countdown display showing remaining time
 * or cycling through numbers. Tagged for countdown / timer /
 * time / animated lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['countdown', 'timer', 'time', 'animated'],
  description: 'An animated countdown display showing remaining time or cycling through numbers.',
};
