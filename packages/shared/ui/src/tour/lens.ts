/**
 * Lens manifest for the Tour component (overlay category) —
 * guided onboarding walkthrough. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['tour', 'onboarding', 'guide', 'walkthrough'],
  description:
    'A guided walkthrough that highlights UI elements with popover explanations to onboard users.',
};
