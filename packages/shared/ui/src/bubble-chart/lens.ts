/**
 * Lens manifest for the BubbleChart component (data-display
 * category) — scatter chart with sized bubble markers. Tagged for
 * bubble / chart / scatter / sized lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['bubble', 'chart', 'scatter', 'sized'],
  description: 'BubbleChart component — awaiting implementation.',
};
