/**
 * Lens manifest for the Lightbox component (overlay category)
 * — full-screen image / media viewer. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['lightbox', 'fullscreen', 'image', 'overlay'],
  description: 'Lightbox component — awaiting implementation.',
};
