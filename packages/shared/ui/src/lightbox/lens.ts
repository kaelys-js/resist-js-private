import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['lightbox', 'fullscreen', 'image', 'overlay'],
  description: 'Lightbox component — awaiting implementation.',
};
