/**
 * Lens manifest for the SkeletonText component (feedback
 * category) — text-shaped loading placeholder lines. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['skeleton', 'text', 'loading'],
  description: 'Text-shaped loading placeholder.',
};
