/**
 * Lens manifest for the AutoLayout component (layout category) —
 * Figma-style auto-layout container that distributes children
 * with consistent spacing rules. Tagged for auto / layout / figma
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['auto', 'layout', 'figma'],
  description: 'Figma-style auto-layout container.',
};
