import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['mobile-stepper', 'dots', 'progress', 'wizard'],
  description: 'MobileStepper component — awaiting implementation.',
};
