import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['knob', 'dial', 'circular-input', 'rotary'],
  description: 'A circular dial/rotary control for selecting numeric values.',
};
