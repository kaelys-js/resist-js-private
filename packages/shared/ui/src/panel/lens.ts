import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['panel', 'container', 'section', 'bordered'],
  description:
    'A collapsible content container with a header, useful for settings or configuration groups.',
};
