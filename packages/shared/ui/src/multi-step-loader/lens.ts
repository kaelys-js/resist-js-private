/**
 * Lens manifest for the MultiStepLoader component (feedback
 * category) — sequential progress loader. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['loader', 'multi-step', 'progress', 'sequential'],
  description: 'MultiStepLoader component — awaiting implementation.',
};
