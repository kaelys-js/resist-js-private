/**
 * Barrel re-export for the separator component — exposes
 * the Separator Svelte component (Bits UI `Separator`
 * wrapper) under stable public names.
 *
 * @module
 */

import Root from './separator.svelte';

export {
  Root,
  //
  Root as Separator,
};
