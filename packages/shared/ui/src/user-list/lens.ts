/**
 * Lens manifest for the UserList component (social category)
 * — avatar + name list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['user', 'list', 'avatar'],
  description: 'User avatar plus name list.',
};
