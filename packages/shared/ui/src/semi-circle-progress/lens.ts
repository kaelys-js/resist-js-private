/**
 * Lens manifest for the SemiCircleProgress component
 * (feedback category) — half-circle progress gauge. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['progress', 'semi-circle', 'gauge', 'arc'],
  description: 'SemiCircleProgress component — awaiting implementation.',
};
