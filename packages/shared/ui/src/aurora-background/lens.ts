/**
 * Lens manifest for the AuroraBackground component (animation
 * category) — ambient gradient background with aurora-style
 * motion. Tagged for aurora / background / gradient / ambient
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['aurora', 'background', 'gradient', 'ambient'],
  description: 'AuroraBackground component — awaiting implementation.',
};
