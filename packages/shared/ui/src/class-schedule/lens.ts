/**
 * Lens manifest for the ClassSchedule component (education
 * category) — class / course timetable schedule view. Tagged for
 * class / schedule / timetable lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['class', 'schedule', 'timetable'],
  description: 'Class timetable schedule.',
};
