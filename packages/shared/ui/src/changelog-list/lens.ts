/**
 * Lens manifest for the ChangelogList component (content
 * category) — release-notes timeline grouped by version. Tagged
 * for changelog / release-notes / timeline / versions lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['changelog', 'release-notes', 'timeline', 'versions'],
  description: 'ChangelogList component — awaiting implementation.',
};
