/**
 * Lens manifest for the PasswordInput component (form
 * category) — password input with show/hide. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['input', 'password', 'toggle-visibility', 'secure'],
  description: 'A password text input with visibility toggle and optional strength indicator.',
};
