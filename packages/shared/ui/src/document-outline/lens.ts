/**
 * Lens manifest for the DocumentOutline component (content
 * category) — auto-generated heading outline. Tagged for
 * document / outline / headings lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['document', 'outline', 'headings'],
  description: 'Document heading outline.',
};
