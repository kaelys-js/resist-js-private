import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['paper', 'surface', 'elevation', 'card-base'],
  description: 'Paper component — awaiting implementation.',
};
