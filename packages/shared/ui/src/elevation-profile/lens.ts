/**
 * Lens manifest for the ElevationProfile component (maps
 * category) — terrain elevation profile chart along a path or
 * route. Tagged for elevation / profile / terrain lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['elevation', 'profile', 'terrain'],
  description: 'Terrain elevation profile chart.',
};
