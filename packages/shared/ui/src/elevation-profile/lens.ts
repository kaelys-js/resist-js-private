import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['elevation', 'profile', 'terrain'],
  description: 'Terrain elevation profile chart.',
};
