import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['highlight', 'text', 'search', 'mark'],
  description:
    'A utility component that highlights matching text within a string, useful for search result display.',
};
