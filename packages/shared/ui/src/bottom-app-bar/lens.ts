/**
 * Lens manifest for the BottomAppBar component (mobile category)
 * — Android-style bottom app bar with FAB notch. Tagged for
 * bottom-app-bar / android / fab-notch / bar lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'mobile',
  tags: ['bottom-app-bar', 'android', 'fab-notch', 'bar'],
  description: 'BottomAppBar component — awaiting implementation.',
};
