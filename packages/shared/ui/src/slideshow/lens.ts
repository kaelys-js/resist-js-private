/**
 * Lens manifest for the Slideshow component (media category)
 * — auto-advancing image slideshow. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['slideshow', 'auto', 'image'],
  description: 'Auto-advancing image slideshow.',
};
