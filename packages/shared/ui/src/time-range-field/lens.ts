import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['time', 'range', 'start-end', 'duration'],
  description: 'TimeRangeField component — awaiting implementation.',
};
