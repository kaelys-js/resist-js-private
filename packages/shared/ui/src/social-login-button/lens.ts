import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['oauth', 'login', 'social', 'button'],
  description: 'OAuth provider login button.',
};
