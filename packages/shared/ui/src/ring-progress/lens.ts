import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['progress', 'ring', 'circular', 'donut'],
  description: 'RingProgress component — awaiting implementation.',
};
