/**
 * Lens manifest for the TypingIndicator component (social
 * category) — animated three-dot typing indicator. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['typing', 'indicator', 'dots', 'animated'],
  description: 'TypingIndicator component — awaiting implementation.',
};
