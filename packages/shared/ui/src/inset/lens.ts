/**
 * Lens manifest for the Inset component (layout category) —
 * content padding utility. Tagged for inset / padding /
 * content-inset / spacing lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['inset', 'padding', 'content-inset', 'spacing'],
  description: 'Inset component — awaiting implementation.',
};
