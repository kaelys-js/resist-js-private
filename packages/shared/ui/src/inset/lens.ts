import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['inset', 'padding', 'content-inset', 'spacing'],
  description: 'Inset component — awaiting implementation.',
};
