/**
 * Lens manifest for the DonutChart component (data-display
 * category) — donut / ring chart for proportional data. Tagged
 * for donut / chart / ring / proportional lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['donut', 'chart', 'ring', 'proportional'],
  description: 'DonutChart component — awaiting implementation.',
};
