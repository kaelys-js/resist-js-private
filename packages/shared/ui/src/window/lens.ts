/**
 * Lens manifest for the Window component (desktop category)
 * — draggable resizable OS-style window. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['window', 'panel', 'draggable', 'resizable'],
  description:
    'A draggable, resizable window panel that mimics desktop OS windows, with title bar, minimize, maximize, and close controls.',
};
