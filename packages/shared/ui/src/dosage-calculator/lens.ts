/**
 * Lens manifest for the DosageCalculator component (healthcare
 * category) — medication dosage calculator. Tagged for dosage
 * / calculator / medication lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['dosage', 'calculator', 'medication'],
  description: 'Medication dosage calculator.',
};
