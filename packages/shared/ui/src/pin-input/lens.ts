import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['pin', 'otp', 'verification', 'code-input'],
  description: 'PinInput component — awaiting implementation.',
};
