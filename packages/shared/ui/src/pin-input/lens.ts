/**
 * Lens manifest for the PinInput component (form category)
 * — multi-cell PIN code input. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['pin', 'otp', 'verification', 'code-input'],
  description: 'PinInput component — awaiting implementation.',
};
