import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['image', 'annotation', 'markup'],
  description: 'Image markup/annotation overlay.',
};
