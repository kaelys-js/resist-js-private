import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['editorial', 'layout', 'columns'],
  description: 'Multi-column editorial layout.',
};
