/**
 * Lens manifest for the SkeletonGroup component (feedback
 * category) — preset skeleton layout mimicking content.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['skeleton', 'loading', 'placeholder', 'group'],
  description:
    'A preset layout of multiple skeleton elements mimicking a specific content type (card, list item, article).',
};
