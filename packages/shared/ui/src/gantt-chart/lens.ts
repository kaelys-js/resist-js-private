import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['gantt', 'chart', 'timeline', 'tasks'],
  description: 'GanttChart component — awaiting implementation.',
};
