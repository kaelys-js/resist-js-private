/**
 * Lens manifest for the AnimatedGradientText component (animation
 * category) — text rendered with a colorful animated gradient
 * fill. Tagged for gradient / text / animated / colorful lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['gradient', 'text', 'animated', 'colorful'],
  description: 'AnimatedGradientText component — awaiting implementation.',
};
