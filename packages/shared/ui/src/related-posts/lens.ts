import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['related', 'posts', 'cards', 'articles'],
  description: 'RelatedPosts component — awaiting implementation.',
};
