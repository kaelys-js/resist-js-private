import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['user', 'presence', 'online-offline', 'dot'],
  description: 'UserPresence component — awaiting implementation.',
};
