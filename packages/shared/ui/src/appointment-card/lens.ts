/**
 * Lens manifest for the AppointmentCard component (healthcare
 * category) — card showing the details of a scheduled medical
 * appointment. Tagged for appointment / card / medical / schedule
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['appointment', 'card', 'medical', 'schedule'],
  description: 'Medical appointment card.',
};
