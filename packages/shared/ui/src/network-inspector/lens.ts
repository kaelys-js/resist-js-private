/**
 * Lens manifest for the NetworkInspector component (devtools
 * category) — devtools-style network inspector. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['network', 'inspector', 'requests'],
  description: 'Network request inspector.',
};
