/**
 * Lens manifest for the BookingCalendar component (scheduling
 * category) — calendar-style date / slot picker for bookings.
 * Tagged for booking / calendar / slots / picker lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['booking', 'calendar', 'slots', 'picker'],
  description: 'BookingCalendar component — awaiting implementation.',
};
