/**
 * Lens manifest for the LabResults component (healthcare
 * category) — medical lab test results display. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['lab', 'results', 'test', 'medical'],
  description: 'Lab test results display.',
};
