/**
 * Lens manifest for the IconSet component (media category) —
 * icon collection browser / picker. Tagged for icon / set /
 * browser / collection lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['icon', 'set', 'browser', 'collection'],
  description: 'Icon collection browser/picker.',
};
