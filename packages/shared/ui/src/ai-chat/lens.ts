/**
 * Lens manifest for the AiChat component (social category) —
 * conversational AI assistant chat surface. Tagged for ai / chat
 * / conversation / assistant lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['ai', 'chat', 'conversation', 'assistant'],
  description: 'AiChat component — awaiting implementation.',
};
