/**
 * Lens manifest for the AboutDialog component — categorizes it
 * under `desktop` with about/dialog/app/info tags for the Lens
 * documentation system.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['about', 'dialog', 'app', 'info'],
  description: 'Application about dialog.',
};
