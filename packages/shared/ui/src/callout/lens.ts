import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['callout', 'info-box', 'highlight', 'attention'],
  description: 'Callout component — awaiting implementation.',
};
