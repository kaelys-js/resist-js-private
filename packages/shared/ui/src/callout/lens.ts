/**
 * Lens manifest for the Callout component (feedback category) —
 * info-box highlight that draws attention to important content.
 * Tagged for callout / info-box / highlight / attention lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['callout', 'info-box', 'highlight', 'attention'],
  description: 'Callout component — awaiting implementation.',
};
