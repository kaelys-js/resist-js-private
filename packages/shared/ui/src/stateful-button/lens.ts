/**
 * Lens manifest for the StatefulButton component (form
 * category) — animated success/error button states. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['button', 'stateful', 'success-error', 'animated'],
  description: 'StatefulButton component — awaiting implementation.',
};
