/**
 * Lens manifest for the FileManager component (data-display
 * category) — file browser / explorer with grid and list views.
 * Tagged for file / manager / browser / explorer lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['file', 'manager', 'browser', 'explorer'],
  description: 'FileManager component — awaiting implementation.',
};
