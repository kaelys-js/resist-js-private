/**
 * Lens manifest for the PieChart component (data-display
 * category) — pie chart with legend. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['pie', 'chart', 'proportional', 'circular'],
  description: 'PieChart component — awaiting implementation.',
};
