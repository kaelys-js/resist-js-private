/**
 * Lens manifest for the CommandPalette component (navigation
 * category) — keyboard-driven command launcher. Tagged for
 * command / palette / launcher lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['command', 'palette', 'launcher'],
  description: 'Keyboard-driven command launcher.',
};
