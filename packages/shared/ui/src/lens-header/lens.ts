import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
	category: 'lens',
	tags: ['header', 'documentation'],
	description: 'Component page header with icon, title, badges, and import path.',
};
