/**
 * Lens manifest for the AgeVerification component (legal category)
 * — age-gate confirmation screen for restricted content. Tagged
 * for age / verification / gate lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'legal',
  tags: ['age', 'verification', 'gate'],
  description: 'Age gate verification screen.',
};
