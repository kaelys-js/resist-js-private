/**
 * Lens manifest for the Footnote component (content category)
 * — inline footnote reference with hover-revealed note. Tagged
 * for footnote / reference / citation lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['footnote', 'reference', 'citation'],
  description: 'Footnote reference and content.',
};
