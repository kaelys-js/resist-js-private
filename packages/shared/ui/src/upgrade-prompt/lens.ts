/**
 * Lens manifest for the UpgradePrompt component (marketing
 * category) — plan upgrade CTA card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['upgrade', 'prompt', 'cta'],
  description: 'Plan upgrade CTA card.',
};
