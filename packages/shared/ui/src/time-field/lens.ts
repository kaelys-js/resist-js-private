/**
 * Lens manifest for the TimeField component (date-time
 * category) — segmented hour/minute time input. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['time', 'field', 'input', 'hour-minute'],
  description: 'TimeField component — awaiting implementation.',
};
