import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['textarea', 'text', 'multiline', 'input'],
  description: 'Multi-line text input with auto-sizing and validation states.',
};
