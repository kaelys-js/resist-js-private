/**
 * Barrel re-export for the textarea component — exposes the
 * Textarea Svelte component under stable public names.
 *
 * @module
 */

import Root from './textarea.svelte';

export {
  Root,
  //
  Root as Textarea,
};
