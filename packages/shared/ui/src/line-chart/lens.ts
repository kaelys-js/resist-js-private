import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['line', 'chart', 'multi-series', 'trend'],
  description: 'LineChart component — awaiting implementation.',
};
