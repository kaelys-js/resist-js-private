import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['compound', 'button', 'secondary-text', 'description'],
  description: 'CompoundButton component — awaiting implementation.',
};
