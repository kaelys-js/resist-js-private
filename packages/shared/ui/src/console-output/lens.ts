import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['console', 'output', 'terminal', 'log'],
  description: 'ConsoleOutput component — awaiting implementation.',
};
