/**
 * Lens manifest for the ConsoleOutput component (admin
 * category) — terminal-style log output panel. Tagged for
 * console / output / terminal / log lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['console', 'output', 'terminal', 'log'],
  description: 'ConsoleOutput component — awaiting implementation.',
};
