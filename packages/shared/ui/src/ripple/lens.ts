/**
 * Lens manifest for the Ripple component (animation
 * category) — animated radial ripple effect. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['ripple', 'click', 'effect', 'material'],
  description:
    'An animated ripple effect emanating from a point, typically used as a background decoration or on button click.',
};
