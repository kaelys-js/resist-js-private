/**
 * Lens manifest for the AnnotationLayer component (utility
 * category) — overlay layer for placing markup annotations on top
 * of content. Tagged for annotation / layer / overlay / markup
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['annotation', 'layer', 'overlay', 'markup'],
  description: 'Content annotation overlay layer.',
};
