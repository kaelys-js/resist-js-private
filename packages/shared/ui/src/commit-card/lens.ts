/**
 * Lens manifest for the CommitCard component (devtools category)
 * — git commit summary card. Tagged for commit / card / git
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['commit', 'card', 'git'],
  description: 'Git commit summary card.',
};
