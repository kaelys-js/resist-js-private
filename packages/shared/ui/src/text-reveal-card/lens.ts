import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['text', 'reveal', 'card', 'spotlight'],
  description: 'TextRevealCard component — awaiting implementation.',
};
