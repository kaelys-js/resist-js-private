/**
 * Lens manifest for the MultiSelect component (form
 * category) — searchable multi-value select. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['select', 'multi', 'tags', 'checkboxes'],
  description:
    'A select input that allows choosing multiple values, often displaying them as chips/tags.',
};
