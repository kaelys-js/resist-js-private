/**
 * Lens manifest for the Barcode component (data-display category)
 * — linear barcode renderer / scanner surface. Tagged for barcode
 * / linear / generator / scanner lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['barcode', 'linear', 'generator', 'scanner'],
  description: 'Barcode component — awaiting implementation.',
};
