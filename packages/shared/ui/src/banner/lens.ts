/**
 * Lens manifest for the Banner component (feedback category) —
 * prominent top/bottom message bar for announcements or promos.
 * Tagged for banner / alert / notification / announcement
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['banner', 'alert', 'notification', 'announcement'],
  description:
    'A prominent message bar displayed at the top or bottom of the page for announcements or promotions.',
};
