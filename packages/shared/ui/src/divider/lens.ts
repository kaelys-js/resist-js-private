import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['divider', 'separator', 'line', 'visual'],
  description:
    'A visual separator between content sections, rendered as a horizontal or vertical line.',
};
