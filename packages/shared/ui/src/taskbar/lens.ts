/**
 * Lens manifest for the Taskbar component (desktop category)
 * — Windows-style taskbar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['taskbar', 'os', 'windows'],
  description: 'Windows-style taskbar.',
};
