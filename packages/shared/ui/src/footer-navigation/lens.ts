/**
 * Lens manifest for the FooterNavigation component (navigation
 * category) — site footer link group. Tagged for footer /
 * navigation / links lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['footer', 'navigation', 'links'],
  description: 'Footer link group navigation.',
};
