/**
 * Lens manifest for the AudioVisualizer component (media category)
 * — real-time audio frequency visualizer. Tagged for audio /
 * visualizer / frequency lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['audio', 'visualizer', 'frequency'],
  description: 'Real-time audio frequency visualizer.',
};
