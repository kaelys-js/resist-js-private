/**
 * Lens manifest for the UrlInput component (form category)
 * — URL input with protocol prefix. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['url', 'input', 'validation'],
  description: 'URL input with protocol prefix.',
};
