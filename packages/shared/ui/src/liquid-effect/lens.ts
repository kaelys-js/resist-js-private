import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['liquid', 'fluid', 'morph'],
  description: 'Liquid/fluid morphing animation.',
};
