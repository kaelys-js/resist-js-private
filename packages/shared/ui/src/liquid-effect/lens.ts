/**
 * Lens manifest for the LiquidEffect component (animation
 * category) — fluid / liquid morph effect. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['liquid', 'fluid', 'morph'],
  description: 'Liquid/fluid morphing animation.',
};
