/**
 * Lens manifest for the BentoGrid component (layout category) —
 * asymmetric mosaic grid layout inspired by Japanese bento boxes.
 * Tagged for grid / bento / dashboard / mosaic lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['grid', 'bento', 'dashboard', 'mosaic'],
  description:
    'A layout component that arranges content in an asymmetric grid inspired by Japanese bento boxes.',
};
