import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['month', 'view', 'calendar', 'grid'],
  description: 'MonthView component — awaiting implementation.',
};
