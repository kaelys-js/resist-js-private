import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['orbit', 'circular', 'rotation', 'animation'],
  description: 'Animated orbital rings with icons or elements rotating around a center point.',
};
