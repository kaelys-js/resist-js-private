/**
 * Lens manifest for the CoolMode component (animation category)
 * — wrapper that spawns small particle / emoji effects on click
 * within the wrapped area. Tagged for particles / click / fun /
 * effect lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['particles', 'click', 'fun', 'effect'],
  description:
    'A wrapper that spawns small particle/emoji effects on every click within the wrapped area.',
};
