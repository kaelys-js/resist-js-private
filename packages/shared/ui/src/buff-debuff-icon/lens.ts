/**
 * Lens manifest for the BuffDebuffIcon component (gaming
 * category) — game status-effect icon with timer overlay. Tagged
 * for buff / debuff / icon / status / game lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['buff', 'debuff', 'icon', 'status', 'game'],
  description: 'Status effect icon with timer.',
};
