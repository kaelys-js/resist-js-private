import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['audit', 'log', 'actions', 'timestamped'],
  description: 'AuditLog component — awaiting implementation.',
};
