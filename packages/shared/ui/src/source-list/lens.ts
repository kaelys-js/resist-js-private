import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['source-list', 'sidebar', 'project', 'nav'],
  description: 'SourceList component — awaiting implementation.',
};
