/**
 * Lens manifest for the BodyMap component (healthcare category)
 * — clickable body-region anatomy diagram. Tagged for body / map
 * / anatomy / diagram lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['body', 'map', 'anatomy', 'diagram'],
  description: 'Clickable body region diagram.',
};
