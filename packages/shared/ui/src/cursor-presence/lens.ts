/**
 * Lens manifest for the CursorPresence component (social
 * category) — collaborative real-time cursors. Tagged for
 * cursor / presence / collaborative lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['cursor', 'presence', 'collaborative'],
  description: 'Collaborative real-time cursors.',
};
