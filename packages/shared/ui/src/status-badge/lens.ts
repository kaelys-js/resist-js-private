import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['badge', 'status', 'indicator', 'dot'],
  description:
    'A small indicator dot or label showing online/offline status, presence, or condition.',
};
