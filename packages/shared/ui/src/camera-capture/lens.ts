/**
 * Lens manifest for the CameraCapture component (media category)
 * — webcam photo-capture surface using `getUserMedia`. Tagged for
 * camera / capture / webcam / photo lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['camera', 'capture', 'webcam', 'photo'],
  description: 'CameraCapture component — awaiting implementation.',
};
