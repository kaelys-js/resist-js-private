/**
 * Lens manifest for the TermsOfService component (legal
 * category) — terms-of-service page render. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'legal',
  tags: ['terms', 'service', 'legal'],
  description: 'Terms of service display.',
};
