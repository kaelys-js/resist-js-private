import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['time', 'picker', 'clock', 'hours-minutes'],
  description:
    'An input component for selecting a time value with hour, minute, and optional second/period fields.',
};
