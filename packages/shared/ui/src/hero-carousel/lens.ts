/**
 * Lens manifest for the HeroCarousel component (marketing
 * category) — hero section with slide rotation. Tagged for
 * hero / carousel / slider lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['hero', 'carousel', 'slider'],
  description: 'Hero section with slide rotation.',
};
