import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['hero', 'carousel', 'slider'],
  description: 'Hero section with slide rotation.',
};
