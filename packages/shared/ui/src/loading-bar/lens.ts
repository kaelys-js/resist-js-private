import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['loading', 'progress', 'bar', 'indeterminate'],
  description:
    'A thin progress bar at the top of the viewport indicating page or async operation loading.',
};
