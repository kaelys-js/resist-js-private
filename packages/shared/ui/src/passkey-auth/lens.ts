/**
 * Lens manifest for the PasskeyAuth component (form
 * category) — passkey authentication. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['passkey', 'webauthn', 'auth', 'biometric'],
  description: 'WebAuthn/passkey authentication.',
};
