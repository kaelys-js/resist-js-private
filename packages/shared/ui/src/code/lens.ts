/**
 * Lens manifest for the Code component (typography category) —
 * inline monospace code element. Tagged for code / inline /
 * monospace / syntax lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['code', 'inline', 'monospace', 'syntax'],
  description: 'Code component — awaiting implementation.',
};
