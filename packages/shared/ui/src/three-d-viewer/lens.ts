/**
 * Lens manifest for the ThreeDViewer component (media
 * category) — 3D model viewer. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['3d', 'model', 'viewer', 'gltf'],
  description: '3D model viewer.',
};
