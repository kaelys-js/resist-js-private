import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['truncate', 'text', 'ellipsis', 'overflow'],
  description:
    'A text component that truncates overflow with an ellipsis, optionally with a "show more" toggle.',
};
