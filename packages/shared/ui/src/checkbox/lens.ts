/**
 * Lens manifest for the Checkbox component (form category) —
 * toggleable boolean input with checked, unchecked, and
 * indeterminate states. Tagged for checkbox / check / toggle /
 * boolean lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['checkbox', 'check', 'toggle', 'boolean'],
  description: 'Toggleable boolean input with checked, unchecked, and indeterminate states.',
};
