import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
	category: 'form',
	tags: ['checkbox', 'check', 'toggle', 'boolean'],
	description: 'Toggleable boolean input with checked, unchecked, and indeterminate states.',
};
