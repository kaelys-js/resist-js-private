import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['feature', 'grid', 'cards', 'benefits'],
  description: 'FeatureGrid component — awaiting implementation.',
};
