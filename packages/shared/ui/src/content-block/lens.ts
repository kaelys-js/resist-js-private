/**
 * Lens manifest for the ContentBlock component (content
 * category) — CMS-style content block. Tagged for content /
 * block / cms lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['content', 'block', 'cms'],
  description: 'CMS content block.',
};
