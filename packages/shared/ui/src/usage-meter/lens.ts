/**
 * Lens manifest for the UsageMeter component (admin
 * category) — resource usage progress bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['usage', 'meter', 'resource'],
  description: 'Resource usage meter/bar.',
};
