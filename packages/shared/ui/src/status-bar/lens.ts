/**
 * Lens manifest for the StatusBar component (desktop
 * category) — OS-style bottom status bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['status', 'bar', 'os', 'desktop'],
  description: 'OS-style status bar.',
};
