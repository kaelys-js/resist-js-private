import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['video', 'player', 'playback', 'controls'],
  description: 'VideoPlayer component — awaiting implementation.',
};
