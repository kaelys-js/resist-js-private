/**
 * Lens manifest for the Fieldset component (form category) —
 * semantic form section grouping with legend. Tagged for
 * fieldset / group / form-section / legend lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['fieldset', 'group', 'form-section', 'legend'],
  description: 'Fieldset component — awaiting implementation.',
};
