/**
 * Lens manifest for the LoadingOverlay component (overlay
 * category) — backdrop overlay with spinner. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['loading', 'overlay', 'spinner', 'blocking'],
  description:
    'A full-area overlay with spinner or progress indicator that covers content during loading.',
};
