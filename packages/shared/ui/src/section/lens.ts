import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['section', 'page-section', 'semantic', 'content-block'],
  description: 'Section component — awaiting implementation.',
};
