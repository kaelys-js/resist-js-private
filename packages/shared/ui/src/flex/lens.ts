/**
 * Lens manifest for the Flex component (layout category) —
 * flexbox layout utility for arranging children. Tagged for
 * flex / layout / container / flexbox lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['flex', 'layout', 'container', 'flexbox'],
  description:
    'A layout utility component that applies CSS flexbox properties to arrange children.',
};
