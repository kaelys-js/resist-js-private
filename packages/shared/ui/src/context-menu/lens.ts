/**
 * Lens manifest for the ContextMenu component (overlay
 * category) — right-click menu with items, checkboxes, and
 * submenus. Tagged for context / menu / right-click / actions
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['context', 'menu', 'right-click', 'actions'],
  description: 'Menu displayed on right-click with items, checkboxes, and submenus.',
};
