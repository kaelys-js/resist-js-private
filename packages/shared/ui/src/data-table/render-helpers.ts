/**
 * Barrel re-export for data-table render helpers — exposes
 * `RenderComponentConfig` / `renderComponent` and
 * `RenderSnippetConfig` / `renderSnippet` so column definitions
 * can declaratively render Svelte components or snippets.
 *
 * @module
 */

export { RenderComponentConfig, renderComponent } from './render-component-config.js';
export { RenderSnippetConfig, renderSnippet } from './render-snippet-config.js';
