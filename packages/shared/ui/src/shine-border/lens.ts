/**
 * Lens manifest for the ShineBorder component (animation
 * category) — animated light-sweep border effect. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['shine', 'border', 'glow', 'gradient'],
  description: 'A decorative border effect with an animated light sweep/shimmer around an element.',
};
