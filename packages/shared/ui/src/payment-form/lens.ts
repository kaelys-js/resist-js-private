import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['payment', 'form', 'card', 'method'],
  description: 'PaymentForm component — awaiting implementation.',
};
