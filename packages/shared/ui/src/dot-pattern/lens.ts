/**
 * Lens manifest for the DotPattern component (animation
 * category) — repeating dot-grid background pattern. Tagged for
 * dot / pattern / background / decorative lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['dot', 'pattern', 'background', 'decorative'],
  description: 'DotPattern component — awaiting implementation.',
};
