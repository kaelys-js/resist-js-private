/**
 * Lens manifest for the EquipmentSlot component (gaming
 * category) — RPG-style inventory equipment slot. Tagged for
 * equipment / slot / gear / rpg lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['equipment', 'slot', 'gear', 'rpg'],
  description: 'Equipment/gear slot.',
};
