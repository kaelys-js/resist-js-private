import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['dialog', 'confirm', 'action', 'destructive'],
  description:
    'A specialized dialog for confirming or canceling an action, with accept/reject buttons.',
};
