/**
 * Lens manifest for the ReadReceipt component (social
 * category) — message read/delivered status indicator.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['read', 'receipt', 'status'],
  description: 'Message read/delivered status.',
};
