/**
 * Lens manifest for the CurrencyDisplay component (finance
 * category) — formatted multi-currency value display. Tagged
 * for currency / display / formatted / multi lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['currency', 'display', 'formatted', 'multi'],
  description: 'CurrencyDisplay component — awaiting implementation.',
};
