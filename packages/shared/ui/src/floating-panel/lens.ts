import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['panel', 'floating', 'draggable', 'window'],
  description:
    'A draggable, resizable floating window that can be moved and sized within the viewport.',
};
