/**
 * Lens manifest for the NavigationMenu compound component
 * (navigation category) — site-wide horizontal menu with
 * dropdowns. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['navigation', 'menu', 'links', 'dropdown', 'tv-variant'],
  description: 'Horizontal navigation with dropdown content panels.',
};
