/**
 * Lens manifest for the ProductGallery component (commerce
 * category) — product image gallery. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['product', 'gallery', 'zoom', 'thumbnails'],
  description: 'ProductGallery component — awaiting implementation.',
};
