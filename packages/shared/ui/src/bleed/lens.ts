/**
 * Lens manifest for the Bleed component (layout category) —
 * negative-margin breakout container that lets content extend
 * beyond its parent's padding. Tagged for bleed / negative-margin
 * / full-width / breakout lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['bleed', 'negative-margin', 'full-width', 'breakout'],
  description: 'Bleed component — awaiting implementation.',
};
