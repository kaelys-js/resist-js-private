/**
 * Lens manifest for the QuantitySelector component (commerce
 * category) — plus/minus quantity counter. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['quantity', 'selector', 'plus-minus', 'counter'],
  description: 'QuantitySelector component — awaiting implementation.',
};
