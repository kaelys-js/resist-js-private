import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['polar', 'chart', 'coordinates', 'circular'],
  description: 'PolarChart component — awaiting implementation.',
};
