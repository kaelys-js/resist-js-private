import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['popconfirm', 'confirm-popover', 'inline-confirm', 'action'],
  description: 'Popconfirm component — awaiting implementation.',
};
