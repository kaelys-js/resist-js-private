/**
 * Lens manifest for the AbsoluteCenter component (layout category)
 * — utility for centering content via absolute positioning.
 * Tagged for absolute-center / overlay-center / positioned /
 * utility lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['absolute-center', 'overlay-center', 'positioned', 'utility'],
  description: 'AbsoluteCenter component — awaiting implementation.',
};
