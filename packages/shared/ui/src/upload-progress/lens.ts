/**
 * Lens manifest for the UploadProgress component (media
 * category) — multi-file upload progress list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['upload', 'progress', 'multi-file', 'list'],
  description: 'UploadProgress component — awaiting implementation.',
};
