import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['upload', 'progress', 'multi-file', 'list'],
  description: 'UploadProgress component — awaiting implementation.',
};
