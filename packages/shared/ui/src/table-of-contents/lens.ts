import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['toc', 'navigation', 'scroll', 'headings'],
  description:
    'An auto-generated navigation list from page headings that highlights the currently visible section.',
};
