/**
 * Lens manifest for the StickyHeader component (navigation
 * category) — sticky scrolling page header. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['header', 'sticky', 'scroll', 'fixed'],
  description:
    'A page header that sticks to the top of the viewport on scroll, with optional show/hide behavior.',
};
