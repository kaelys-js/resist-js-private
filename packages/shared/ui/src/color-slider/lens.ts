/**
 * Lens manifest for the ColorSlider component (form category) —
 * single-channel color slider (hue / saturation / lightness /
 * alpha). Tagged for color / slider / hue / channel lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'slider', 'hue', 'channel'],
  description: 'ColorSlider component — awaiting implementation.',
};
