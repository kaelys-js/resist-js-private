/**
 * Lens manifest for the LongPress component (utility
 * category) — long-press gesture detector. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['long-press', 'hold', 'gesture'],
  description: 'Long press/hold gesture detection.',
};
