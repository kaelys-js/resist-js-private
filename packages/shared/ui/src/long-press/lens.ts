import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['long-press', 'hold', 'gesture'],
  description: 'Long press/hold gesture detection.',
};
