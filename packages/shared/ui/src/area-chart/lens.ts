/**
 * Lens manifest for the AreaChart component (data-display
 * category) — trend chart with a filled area beneath the line.
 * Tagged for area / chart / trend / filled-line lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['area', 'chart', 'trend', 'filled-line'],
  description: 'AreaChart component — awaiting implementation.',
};
