/**
 * Lens manifest for the CurrencyInput component (form
 * category) — monetary value input with currency symbol.
 * Tagged for currency / money / input lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['currency', 'money', 'input'],
  description: 'Monetary value input with currency symbol.',
};
