/**
 * Lens manifest for the ForgotPasswordForm component (form
 * category) — password reset request form. Tagged for password
 * / reset / auth / form lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['password', 'reset', 'auth', 'form'],
  description: 'Password reset request form.',
};
