import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['keyboard', 'shortcut', 'help', 'overlay'],
  description: 'KeyboardShortcutHelp component — awaiting implementation.',
};
