/**
 * Lens manifest for the InvoiceTable component (finance
 * category) — itemized invoice with totals. Tagged for invoice
 * / table / itemized / totals lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['invoice', 'table', 'itemized', 'totals'],
  description: 'InvoiceTable component — awaiting implementation.',
};
