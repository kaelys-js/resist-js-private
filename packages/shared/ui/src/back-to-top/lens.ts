import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['scroll', 'top', 'button', 'navigation'],
  description:
    'A button that appears after scrolling down and smoothly scrolls the page back to the top.',
};
