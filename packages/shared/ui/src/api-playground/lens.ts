import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['api', 'playground', 'request', 'builder'],
  description: 'ApiPlayground component — awaiting implementation.',
};
