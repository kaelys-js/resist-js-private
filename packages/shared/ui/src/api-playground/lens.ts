/**
 * Lens manifest for the ApiPlayground component (admin category)
 * — interactive request builder for testing API endpoints. Tagged
 * for api / playground / request / builder lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['api', 'playground', 'request', 'builder'],
  description: 'ApiPlayground component — awaiting implementation.',
};
