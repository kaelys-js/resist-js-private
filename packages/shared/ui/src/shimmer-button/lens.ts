/**
 * Lens manifest for the ShimmerButton component (animation
 * category) — animated shine-sweep CTA. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['shimmer', 'button', 'glow', 'gradient'],
  description: 'A button with an animated shimmer/shine sweep effect across its surface.',
};
