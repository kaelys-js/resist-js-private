/**
 * Lens manifest for the SlideGroup component (layout
 * category) — horizontally scrollable item group. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['slide', 'group', 'horizontal', 'scrollable'],
  description: 'SlideGroup component — awaiting implementation.',
};
