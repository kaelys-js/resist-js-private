import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['comparison', 'table', 'feature', 'matrix'],
  description: 'ComparisonTable component — awaiting implementation.',
};
