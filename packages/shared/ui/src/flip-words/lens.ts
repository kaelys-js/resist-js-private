import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['flip', 'words', 'rotating-text', 'typewriter'],
  description: 'FlipWords component — awaiting implementation.',
};
