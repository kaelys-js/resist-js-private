import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['coming-soon', 'pre-launch', 'countdown', 'page'],
  description: 'ComingSoon component — awaiting implementation.',
};
