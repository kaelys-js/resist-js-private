/**
 * Lens manifest for the ComingSoon component (marketing
 * category) — pre-launch countdown / coming-soon page. Tagged
 * for coming-soon / pre-launch / countdown / page lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['coming-soon', 'pre-launch', 'countdown', 'page'],
  description: 'ComingSoon component — awaiting implementation.',
};
