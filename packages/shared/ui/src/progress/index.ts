/**
 * Barrel re-export for the progress component — exposes the
 * Progress Svelte component (Bits UI `Progress` wrapper) under
 * stable public names.
 *
 * @module
 */

import Root from './progress.svelte';

export {
  Root,
  //
  Root as Progress,
};
