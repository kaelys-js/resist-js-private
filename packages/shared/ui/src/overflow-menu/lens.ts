/**
 * Lens manifest for the OverflowMenu component (overlay
 * category) — three-dots overflow menu. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['overflow', 'menu', 'collapse', 'more'],
  description: 'OverflowMenu component — awaiting implementation.',
};
