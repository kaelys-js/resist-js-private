/**
 * Lens manifest for the Masonry component (layout category)
 * — Pinterest-style staggered grid. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['masonry', 'grid', 'layout', 'pinterest'],
  description:
    'A layout component that arranges items in a staggered grid where items fill vertical space optimally, like bricks in a wall.',
};
