/**
 * Lens manifest for the Fireworks component (animation
 * category) — celebratory particle explosion effect. Tagged
 * for fireworks / particles / explosion lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['fireworks', 'particles', 'explosion'],
  description: 'Fireworks particle explosion.',
};
