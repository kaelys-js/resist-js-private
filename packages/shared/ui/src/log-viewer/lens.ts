import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['log', 'viewer', 'scrollable', 'output'],
  description: 'LogViewer component — awaiting implementation.',
};
