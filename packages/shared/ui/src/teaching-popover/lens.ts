import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['teaching', 'popover', 'instructional', 'step'],
  description: 'TeachingPopover component — awaiting implementation.',
};
