/**
 * Lens manifest for the HoverCard compound component (overlay
 * category) — card that appears on hover with a preview of
 * linked content. Tagged for hover / card / preview / popover
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['hover', 'card', 'preview', 'popover'],
  description: 'Card that appears on hover with a preview of linked content.',
};
