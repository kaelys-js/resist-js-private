import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
	category: 'overlay',
	tags: ['hover', 'card', 'preview', 'popover'],
	description: 'Card that appears on hover with a preview of linked content.',
};
