/**
 * Lens manifest for the FilePreview component (media category)
 * — thumbnail preview card with type-aware rendering. Tagged
 * for file / preview / thumbnail / type lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['file', 'preview', 'thumbnail', 'type'],
  description: 'FilePreview component — awaiting implementation.',
};
