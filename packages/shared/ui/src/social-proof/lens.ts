/**
 * Lens manifest for the SocialProof component (marketing
 * category) — recent-activity trust notification. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['social-proof', 'notification', 'recent-activity', 'trust'],
  description: 'SocialProof component — awaiting implementation.',
};
