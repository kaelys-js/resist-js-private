import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['icon', 'browser', 'searchable', 'grid'],
  description: 'IconBrowser component — awaiting implementation.',
};
