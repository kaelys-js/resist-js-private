/**
 * Lens manifest for the IconBrowser component (data-display
 * category) — searchable icon library grid. Tagged for icon /
 * browser / searchable / grid lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['icon', 'browser', 'searchable', 'grid'],
  description: 'IconBrowser component — awaiting implementation.',
};
