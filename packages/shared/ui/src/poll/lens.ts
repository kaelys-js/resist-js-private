/**
 * Lens manifest for the Poll component (social category) —
 * voting poll widget. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['poll', 'vote', 'options', 'results'],
  description: 'Poll component — awaiting implementation.',
};
