/**
 * Lens manifest for the Clipboard component (utility category) —
 * helper for copying text to the clipboard with visual feedback.
 * Tagged for clipboard / copy / paste / text lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['clipboard', 'copy', 'paste', 'text'],
  description: 'A utility component for copying text to the clipboard with visual feedback.',
};
