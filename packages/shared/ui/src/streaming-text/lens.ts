/**
 * Lens manifest for the StreamingText component (social
 * category) — token-streaming typewriter text. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['streaming', 'text', 'token', 'typewriter'],
  description: 'StreamingText component — awaiting implementation.',
};
