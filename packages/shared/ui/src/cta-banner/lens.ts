import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['cta', 'banner', 'call-to-action', 'conversion'],
  description: 'CtaBanner component — awaiting implementation.',
};
