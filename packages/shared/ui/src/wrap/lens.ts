import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['wrap', 'flex-wrap', 'flow', 'responsive'],
  description: 'Wrap component — awaiting implementation.',
};
