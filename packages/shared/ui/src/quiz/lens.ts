/**
 * Lens manifest for the Quiz component (education category)
 * — interactive quiz/assessment. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['quiz', 'test', 'assessment'],
  description: 'Interactive quiz/test component.',
};
