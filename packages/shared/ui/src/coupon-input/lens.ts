/**
 * Lens manifest for the CouponInput component (commerce
 * category) — promo / coupon code input with apply button.
 * Tagged for coupon / promo / input / code lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['coupon', 'promo', 'input', 'code'],
  description: 'Promo/coupon code input with apply.',
};
