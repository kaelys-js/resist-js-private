/**
 * Lens manifest for the ViewTransition component (animation
 * category) — View Transition API wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['view', 'transition', 'api'],
  description: 'View Transition API wrapper.',
};
