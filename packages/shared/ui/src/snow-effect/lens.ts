import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['snow', 'particles', 'overlay'],
  description: 'Falling snow particle overlay.',
};
