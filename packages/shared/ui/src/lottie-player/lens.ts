/**
 * Lens manifest for the LottiePlayer component (animation
 * category) — Lottie animation player. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['lottie', 'animation', 'player'],
  description: 'Lottie JSON animation player.',
};
