/**
 * Lens manifest for the NotificationBadge component
 * (feedback category) — count badge on notification icon.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['notification', 'badge', 'count'],
  description: 'Numeric count bubble overlay.',
};
