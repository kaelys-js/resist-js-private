/**
 * Lens manifest for the AnimatedBeam component (animation
 * category) — animated SVG-path beam connecting two anchor
 * elements. Tagged for beam / animated / connection / svg-path
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['beam', 'animated', 'connection', 'svg-path'],
  description: 'AnimatedBeam component — awaiting implementation.',
};
