import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['pull-to-refresh', 'mobile', 'gesture', 'reload'],
  description: 'PullToRefresh component — awaiting implementation.',
};
