/**
 * Lens manifest for the ParticlesBg component (animation
 * category) — particle backdrop. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['particles', 'background', 'floating', 'interactive'],
  description: 'ParticlesBg component — awaiting implementation.',
};
