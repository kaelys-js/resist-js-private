import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['page-sheet', 'modal-card', 'ios', 'slide-up'],
  description: 'PageSheet component — awaiting implementation.',
};
