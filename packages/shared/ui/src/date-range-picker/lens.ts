/**
 * Lens manifest for the DateRangePicker component (date-time
 * category) — date picker specifically for selecting a start /
 * end date range. Tagged for date / range / picker / start-end
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['date', 'range', 'picker', 'start-end'],
  description: 'A date picker specifically designed for selecting a start and end date range.',
};
