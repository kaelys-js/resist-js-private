/**
 * Lens manifest for the PathBar component (navigation
 * category) — file-system breadcrumb bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['path-bar', 'breadcrumb', 'file', 'bar'],
  description: 'PathBar component — awaiting implementation.',
};
