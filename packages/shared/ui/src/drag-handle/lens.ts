/**
 * Lens manifest for the DragHandle component (utility category)
 * — draggable handle grip element. Tagged for drag / handle /
 * grip lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['drag', 'handle', 'grip'],
  description: 'Draggable handle grip element.',
};
