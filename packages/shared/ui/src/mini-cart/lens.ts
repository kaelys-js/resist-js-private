/**
 * Lens manifest for the MiniCart component (commerce
 * category) — compact cart preview popover. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['mini', 'cart', 'dropdown'],
  description: 'Compact cart dropdown.',
};
