/**
 * Lens manifest for the SvgMaskEffect component (animation
 * category) — animated SVG-mask reveal. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['svg', 'mask', 'reveal', 'clip-path'],
  description: 'SvgMaskEffect component — awaiting implementation.',
};
