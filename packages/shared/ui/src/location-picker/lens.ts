import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['location', 'picker', 'map', 'coordinates'],
  description: 'LocationPicker component — awaiting implementation.',
};
