/**
 * Lens manifest for the EmptyState component (feedback
 * category) — placeholder display shown when a section has no
 * content, with optional illustration and call-to-action button.
 * Tagged for empty / placeholder / no-data / illustration
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['empty', 'placeholder', 'no-data', 'illustration'],
  description:
    'A placeholder display shown when a section has no content, with optional illustration and action.',
};
