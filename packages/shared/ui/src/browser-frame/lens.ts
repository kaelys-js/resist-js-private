/**
 * Lens manifest for the BrowserFrame component (media category)
 * — decorative wrapper rendering content inside a realistic
 * browser window frame with address bar, tabs, and window
 * controls. Tagged for mockup / browser / frame / screenshot
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['mockup', 'browser', 'frame', 'screenshot'],
  description:
    'A decorative wrapper that renders content inside a realistic browser window frame with address bar, tabs, and window controls.',
};
