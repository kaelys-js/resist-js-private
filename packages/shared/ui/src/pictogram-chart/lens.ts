/**
 * Lens manifest for the PictogramChart component
 * (data-display category) — pictogram proportion chart.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['pictogram', 'icon', 'chart'],
  description: 'Icon-based proportional chart.',
};
