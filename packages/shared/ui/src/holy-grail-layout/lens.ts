/**
 * Lens manifest for the HolyGrailLayout component (layout
 * category) — classic header / sidebar / content / footer
 * layout. Tagged for holy-grail / layout / classic lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['holy-grail', 'layout', 'classic'],
  description: 'Classic header/sidebar/content/footer layout.',
};
