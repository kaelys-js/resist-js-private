/**
 * Lens manifest for the AuroraText component (animation category)
 * — text rendered with a shimmering aurora-style gradient fill.
 * Tagged for aurora / text / gradient / shimmering lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['aurora', 'text', 'gradient', 'shimmering'],
  description: 'AuroraText component — awaiting implementation.',
};
