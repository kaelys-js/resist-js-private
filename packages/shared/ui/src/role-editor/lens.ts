/**
 * Lens manifest for the RoleEditor component (admin
 * category) — permission role editor. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['role', 'permission', 'editor'],
  description: 'Permission role editor.',
};
