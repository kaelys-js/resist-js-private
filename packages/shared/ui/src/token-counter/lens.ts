/**
 * Lens manifest for the TokenCounter component (social
 * category) — token usage meter. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['token', 'counter', 'usage', 'meter'],
  description: 'TokenCounter component — awaiting implementation.',
};
