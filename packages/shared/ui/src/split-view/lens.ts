/**
 * Lens manifest for the SplitView component (desktop
 * category) — resizable dual-pane split view. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['split', 'view', 'dual-pane'],
  description: 'Resizable dual-pane split view.',
};
