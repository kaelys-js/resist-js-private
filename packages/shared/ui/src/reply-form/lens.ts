/**
 * Lens manifest for the ReplyForm component (social
 * category) — comment/reply compose form. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['reply', 'comment', 'form'],
  description: 'Reply/comment compose form.',
};
