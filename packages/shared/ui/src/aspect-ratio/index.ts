/**
 * Barrel re-export for the aspect-ratio component — exposes the
 * `AspectRatio` Svelte component under both the internal `Root`
 * alias and the public `AspectRatio` name.
 *
 * @module
 */

import Root from './aspect-ratio.svelte';

export { Root, Root as AspectRatio };
