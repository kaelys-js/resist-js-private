/**
 * Lens manifest for the SocialShare component (social
 * category) — share menu for social platforms. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['social', 'share', 'platforms', 'buttons'],
  description: 'SocialShare component — awaiting implementation.',
};
