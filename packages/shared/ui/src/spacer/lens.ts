/**
 * Lens manifest for the Spacer component (layout category)
 * — flexible empty-space sizing element. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['spacer', 'gap', 'layout', 'whitespace'],
  description: 'A flexible empty space component for adding consistent spacing between elements.',
};
