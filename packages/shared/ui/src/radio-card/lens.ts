import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['radio', 'card', 'selection', 'visual-picker'],
  description: 'RadioCard component — awaiting implementation.',
};
