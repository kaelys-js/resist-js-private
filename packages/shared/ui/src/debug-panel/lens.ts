/**
 * Lens manifest for the DebugPanel component (devtools
 * category) — developer debug panel. Tagged for debug / panel /
 * developer lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['debug', 'panel', 'developer'],
  description: 'Developer debug panel.',
};
