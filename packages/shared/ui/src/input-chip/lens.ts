/**
 * Lens manifest for the InputChip component (form category) —
 * tokenised multi-value input. Tagged for input / chip /
 * removable / token lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['input', 'chip', 'removable', 'token'],
  description: 'InputChip component — awaiting implementation.',
};
