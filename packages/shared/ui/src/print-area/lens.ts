/**
 * Lens manifest for the PrintArea component (utility
 * category) — printable region wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['print', 'area', 'content'],
  description: 'Print-specific content wrapper.',
};
