/**
 * Lens manifest for the Dock component (desktop category) —
 * macOS-style navigation bar with icon magnification on hover
 * proximity. Tagged for dock / taskbar / launcher / macos
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['dock', 'taskbar', 'launcher', 'macos'],
  description: 'A macOS-style navigation bar with icon magnification on hover proximity.',
};
