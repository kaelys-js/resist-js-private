/**
 * Lens manifest for the SplitterPanel component (layout
 * category) — generic resizable split-pane wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['splitter', 'split-pane', 'resizable', 'divider'],
  description: 'SplitterPanel component — awaiting implementation.',
};
