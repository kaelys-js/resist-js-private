/**
 * Lens manifest for the CommentThread component (social
 * category) — nested comments / replies thread. Tagged for
 * comment / thread / nested / replies lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['comment', 'thread', 'nested', 'replies'],
  description: 'CommentThread component — awaiting implementation.',
};
