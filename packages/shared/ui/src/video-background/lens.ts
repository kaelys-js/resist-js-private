/**
 * Lens manifest for the VideoBackground component (media
 * category) — fullscreen ambient video. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['video', 'background', 'fullscreen', 'ambient'],
  description: 'VideoBackground component — awaiting implementation.',
};
