/**
 * Lens manifest for the SankeyDiagram component
 * (data-display category) — flow / alluvial diagram. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['sankey', 'diagram', 'flow', 'alluvial'],
  description: 'SankeyDiagram component — awaiting implementation.',
};
