/**
 * Lens manifest for the GlowEffect component (animation
 * category) — ambient hover glow wrapper. Tagged for glow /
 * effect / hover / ambient lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['glow', 'effect', 'hover', 'ambient'],
  description: 'GlowEffect component — awaiting implementation.',
};
