/**
 * Lens manifest for the Hotkey component (utility category) —
 * keyboard shortcut binding utility. Tagged for hotkey /
 * keyboard / shortcut lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['hotkey', 'keyboard', 'shortcut'],
  description: 'Keyboard shortcut binding utility.',
};
