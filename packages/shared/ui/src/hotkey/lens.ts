import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['hotkey', 'keyboard', 'shortcut'],
  description: 'Keyboard shortcut binding utility.',
};
