import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
	category: 'lens',
	tags: ['preview', 'default'],
	description: 'Default variant preview card for Lens documentation pages.',
};
