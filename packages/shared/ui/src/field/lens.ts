/**
 * Lens manifest for the Field compound component (form
 * category) — structured form field with label, description,
 * error message, and content slots. Tagged for field /
 * form-field / label / validation / tv-variant lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['field', 'form-field', 'label', 'validation', 'tv-variant'],
  description: 'Structured form field with label, description, error message, and content slots.',
};
