/**
 * Lens manifest for the ConfidenceMeter component (display
 * category) — confidence / probability gauge for AI outputs.
 * Tagged for confidence / meter / probability / gauge lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['confidence', 'meter', 'probability', 'gauge'],
  description: 'ConfidenceMeter component — awaiting implementation.',
};
