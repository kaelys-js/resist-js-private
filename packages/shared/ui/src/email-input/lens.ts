import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['email', 'input', 'validation'],
  description: 'Email address input with validation.',
};
