/**
 * Lens manifest for the EmailInput component (form category) —
 * email address input with built-in validation styling. Tagged
 * for email / input / validation lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['email', 'input', 'validation'],
  description: 'Email address input with validation.',
};
