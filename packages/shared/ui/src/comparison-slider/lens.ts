/**
 * Lens manifest for the ComparisonSlider component (media
 * category) — interactive before/after image comparison with a
 * draggable divider. Tagged for slider / comparison /
 * before-after / image lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['slider', 'comparison', 'before-after', 'image'],
  description: 'An interactive before/after image comparison with a draggable divider.',
};
