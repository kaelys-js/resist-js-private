/**
 * Lens manifest for the PickList component (form category) —
 * dual-list selector. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['picklist', 'dual-list', 'selection', 'transfer'],
  description: 'A dual-list transfer component for moving items between source and target lists.',
};
