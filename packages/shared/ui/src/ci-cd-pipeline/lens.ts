/**
 * Lens manifest for the CiCdPipeline component (devtools
 * category) — CI/CD pipeline stage status display. Tagged for
 * ci-cd / pipeline / stage lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['ci-cd', 'pipeline', 'stage'],
  description: 'CI/CD pipeline stage display.',
};
