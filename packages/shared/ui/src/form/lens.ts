/**
 * Lens manifest for the Form compound component (form
 * category) — Formsnap-driven form field wrapper with label,
 * description, and validation message slots. Tagged for form /
 * validation / field / submit lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['form', 'validation', 'field', 'submit'],
  description: 'Form field wrapper with label, description, and validation message slots.',
};
