/**
 * Lens manifest for the ExperienceBar component (gaming
 * category) — RPG-style XP / level progress bar. Tagged for
 * experience / bar / xp / level lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['experience', 'bar', 'xp', 'level'],
  description: 'ExperienceBar component — awaiting implementation.',
};
