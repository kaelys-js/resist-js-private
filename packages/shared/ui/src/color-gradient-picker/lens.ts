/**
 * Lens manifest for the ColorGradientPicker component (form
 * category) — multi-stop CSS gradient editor. Tagged for color
 * / gradient / stops / editor lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'gradient', 'stops', 'editor'],
  description: 'ColorGradientPicker component — awaiting implementation.',
};
