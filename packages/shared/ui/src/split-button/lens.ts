/**
 * Lens manifest for the SplitButton component (form
 * category) — primary button + dropdown of secondary
 * actions. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['button', 'split', 'dropdown', 'action-menu'],
  description:
    'A button combined with a dropdown trigger, providing a primary action and secondary options.',
};
