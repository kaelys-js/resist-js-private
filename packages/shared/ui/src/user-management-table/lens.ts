/**
 * Lens manifest for the UserManagementTable component
 * (admin category) — admin user table with actions. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['user', 'management', 'admin', 'table'],
  description: 'User admin table with actions.',
};
