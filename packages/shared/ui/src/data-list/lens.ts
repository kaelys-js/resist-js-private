/**
 * Lens manifest for the DataList component (data-display
 * category) — definition-list / key-value pairs display. Tagged
 * for data-list / key-value / definition / pairs lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['data-list', 'key-value', 'definition', 'pairs'],
  description: 'DataList component — awaiting implementation.',
};
