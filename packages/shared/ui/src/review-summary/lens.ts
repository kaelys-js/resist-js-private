/**
 * Lens manifest for the ReviewSummary component (marketing
 * category) — aggregate review rating with bar breakdown.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['review', 'summary', 'aggregate'],
  description: 'Aggregate rating with bar breakdown.',
};
