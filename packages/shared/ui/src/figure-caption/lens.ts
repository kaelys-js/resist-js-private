/**
 * Lens manifest for the FigureCaption component (content
 * category) — image / figure with caption. Tagged for figure /
 * caption / image lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['figure', 'caption', 'image'],
  description: 'Image/figure with caption.',
};
