/**
 * Lens manifest for the FaqAccordion component (marketing
 * category) — collapsible FAQ list. Tagged for faq / accordion /
 * questions / help lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['faq', 'accordion', 'questions', 'help'],
  description: 'FaqAccordion component — awaiting implementation.',
};
