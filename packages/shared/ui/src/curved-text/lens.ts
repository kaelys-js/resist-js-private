/**
 * Lens manifest for the CurvedText component (animation
 * category) — text laid out along an SVG arc / circular path.
 * Tagged for curved / text / svg-path / circular lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['curved', 'text', 'svg-path', 'circular'],
  description: 'CurvedText component — awaiting implementation.',
};
