/**
 * Lens manifest for the Citation component (content category) —
 * academic / source citation block. Tagged for citation / source
 * / academic lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['citation', 'source', 'academic'],
  description: 'Academic/source citation.',
};
