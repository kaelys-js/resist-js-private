/**
 * Lens manifest for the BlogCard component (marketing category)
 * — blog post preview card with title, image, and excerpt.
 * Tagged for blog / card / article / post-preview lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['blog', 'card', 'article', 'post-preview'],
  description: 'BlogCard component — awaiting implementation.',
};
