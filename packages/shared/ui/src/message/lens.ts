/**
 * Lens manifest for the Message component (feedback
 * category) — generic message banner. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['message', 'alert', 'inline', 'notification'],
  description:
    'A lightweight feedback display that appears at the top of the page for brief notifications.',
};
