/**
 * Lens manifest for the RetroGrid component (animation
 * category) — 80s-style perspective grid floor effect.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['retro', 'grid', 'background', 'perspective'],
  description:
    'A CSS-based perspective grid that creates an 80s/retro-style vanishing point floor effect.',
};
