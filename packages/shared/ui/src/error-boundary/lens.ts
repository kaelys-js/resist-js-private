import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['error', 'boundary', 'fallback'],
  description: 'Error catching fallback wrapper.',
};
