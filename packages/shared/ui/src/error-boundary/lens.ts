/**
 * Lens manifest for the ErrorBoundary component (utility
 * category) — error-catching fallback wrapper. Tagged for error
 * / boundary / fallback lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['error', 'boundary', 'fallback'],
  description: 'Error catching fallback wrapper.',
};
