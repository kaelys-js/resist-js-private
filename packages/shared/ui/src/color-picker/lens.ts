/**
 * Lens manifest for the ColorPicker component (form category) —
 * compact color picker with hex text input and native color
 * swatch. Tagged for color / picker / input / hex / swatch
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'picker', 'input', 'hex', 'swatch'],
  description: 'Compact color picker with hex text input and native color swatch.',
};
