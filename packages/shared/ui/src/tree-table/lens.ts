import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['tree', 'table', 'hierarchical', 'expandable-rows'],
  description: 'TreeTable component — awaiting implementation.',
};
