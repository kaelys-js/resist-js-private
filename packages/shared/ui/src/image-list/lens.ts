/**
 * Lens manifest for the ImageList component (media category)
 * — gallery-grid display of an image collection. Tagged for
 * image / list / gallery / grid lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['image', 'list', 'gallery', 'grid'],
  description: 'ImageList component — awaiting implementation.',
};
