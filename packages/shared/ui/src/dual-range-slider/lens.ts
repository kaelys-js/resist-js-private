/**
 * Lens manifest for the DualRangeSlider component (form
 * category) — slider with two thumbs for min/max range
 * selection. Tagged for slider / range / dual-thumb / min-max
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['slider', 'range', 'dual-thumb', 'min-max'],
  description: 'DualRangeSlider component — awaiting implementation.',
};
