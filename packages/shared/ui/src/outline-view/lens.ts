import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['outline', 'tree', 'sidebar'],
  description: 'Document/tree outline sidebar.',
};
