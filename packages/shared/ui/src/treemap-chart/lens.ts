/**
 * Lens manifest for the TreemapChart component (data-display
 * category) — nested rectangle treemap. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['treemap', 'chart', 'nested', 'rectangles'],
  description: 'TreemapChart component — awaiting implementation.',
};
