import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'a11y',
  tags: ['screen-reader', 'a11y', 'hidden', 'text'],
  description: 'ScreenReaderOnly component — awaiting implementation.',
};
