/**
 * Lens manifest for the CategoryBar component (data-display
 * category) — proportional segmented bar showing category shares.
 * Tagged for category / bar / segmented / proportional lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['category', 'bar', 'segmented', 'proportional'],
  description: 'CategoryBar component — awaiting implementation.',
};
