import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['cta', 'section', 'signup', 'conversion'],
  description: 'CtaSection component — awaiting implementation.',
};
