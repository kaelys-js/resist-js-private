/**
 * Lens manifest for the CtaSection component (marketing
 * category) — full-section call-to-action with signup. Tagged
 * for cta / section / signup / conversion lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['cta', 'section', 'signup', 'conversion'],
  description: 'CtaSection component — awaiting implementation.',
};
