import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['spark', 'chart', 'mini', 'inline-graph'],
  description: 'SparkChart component — awaiting implementation.',
};
