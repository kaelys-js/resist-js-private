import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['share-sheet', 'share-menu', 'social', 'mobile'],
  description: 'ShareSheet component — awaiting implementation.',
};
