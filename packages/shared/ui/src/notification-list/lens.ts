import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['notification', 'list', 'feed', 'inbox'],
  description:
    'A stacked notification display showing alerts, messages, or activity items with animations.',
};
