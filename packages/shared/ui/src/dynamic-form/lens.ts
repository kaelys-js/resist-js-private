/**
 * Lens manifest for the DynamicForm component (form category)
 * — schema-driven form builder that renders fields from a
 * runtime schema definition. Tagged for dynamic / schema / form /
 * builder lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['dynamic', 'schema', 'form', 'builder'],
  description: 'Schema-driven dynamic form builder.',
};
