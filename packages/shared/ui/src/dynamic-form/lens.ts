import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['dynamic', 'schema', 'form', 'builder'],
  description: 'Schema-driven dynamic form builder.',
};
