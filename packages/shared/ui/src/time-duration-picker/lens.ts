import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['time', 'duration', 'picker', 'hms'],
  description: 'TimeDurationPicker component — awaiting implementation.',
};
