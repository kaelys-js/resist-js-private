/**
 * Lens manifest for the DeliveryStatus component (data-display
 * category) — shipping / delivery step tracker. Tagged for
 * delivery / shipping / tracker lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['delivery', 'shipping', 'tracker'],
  description: 'Shipping/delivery step tracker.',
};
