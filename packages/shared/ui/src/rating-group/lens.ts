import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['rating', 'stars', 'score', 'review'],
  description: 'A star/icon-based rating input for user reviews and feedback.',
};
