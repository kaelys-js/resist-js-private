/**
 * Lens manifest for the QueryEditor component (devtools
 * category) — SQL/GraphQL query editor surface. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['query', 'sql', 'graphql', 'editor'],
  description: 'SQL/GraphQL query editor.',
};
