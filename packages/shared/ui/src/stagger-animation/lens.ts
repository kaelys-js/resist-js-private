import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['stagger', 'animation', 'entrance'],
  description: 'Staggered children entrance animation.',
};
