import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'lens',
  tags: ['dependencies', 'tree', 'imports', 'graph'],
  description:
    'Categorized dependency tree visualization with collapsible sections and component links.',
};
