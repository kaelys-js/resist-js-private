/**
 * Lens manifest for the DamageNumber component (gaming
 * category) — floating damage number popup. Tagged for damage
 * / number / popup / game lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['damage', 'number', 'popup', 'game'],
  description: 'Floating damage number popup.',
};
