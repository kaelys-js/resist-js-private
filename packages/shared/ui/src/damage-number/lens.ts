import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['damage', 'number', 'popup', 'game'],
  description: 'Floating damage number popup.',
};
