import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['checkbox', 'group', 'multi-select', 'form-control'],
  description: 'CheckboxGroup component — awaiting implementation.',
};
