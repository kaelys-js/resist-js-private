/**
 * Lens manifest for the CheckboxGroup component (form category)
 * — grouped multi-select checkboxes wired as a single form
 * control. Tagged for checkbox / group / multi-select /
 * form-control lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['checkbox', 'group', 'multi-select', 'form-control'],
  description: 'CheckboxGroup component — awaiting implementation.',
};
