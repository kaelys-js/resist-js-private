/**
 * Lens manifest for the GestureHandler component (mobile
 * category) — multi-touch gesture detection. Tagged for
 * gesture / handler / touch / mobile lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'mobile',
  tags: ['gesture', 'handler', 'touch', 'mobile'],
  description: 'Multi-touch gesture detection.',
};
