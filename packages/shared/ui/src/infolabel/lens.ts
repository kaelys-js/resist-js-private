import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['info', 'label', 'popover', 'icon'],
  description: 'Infolabel component — awaiting implementation.',
};
