import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['permission', 'matrix', 'role', 'checkbox-grid'],
  description: 'PermissionMatrix component — awaiting implementation.',
};
