/**
 * Lens manifest for the PermissionMatrix component (admin
 * category) — role / permission grid. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['permission', 'matrix', 'role', 'checkbox-grid'],
  description: 'PermissionMatrix component — awaiting implementation.',
};
