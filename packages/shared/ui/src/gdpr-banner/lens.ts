/**
 * Lens manifest for the GdprBanner component (legal category)
 * — GDPR compliance notification bar. Tagged for gdpr / privacy
 * / banner / compliance lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'legal',
  tags: ['gdpr', 'privacy', 'banner', 'compliance'],
  description: 'GDPR compliance notification bar.',
};
