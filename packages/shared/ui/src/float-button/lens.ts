/**
 * Lens manifest for the FloatButton component (navigation
 * category) — floating action button (FAB). Tagged for fab /
 * floating / action / button lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['fab', 'floating', 'action', 'button'],
  description:
    'A floating action button (FAB) fixed to the viewport, optionally with a group of related actions.',
};
