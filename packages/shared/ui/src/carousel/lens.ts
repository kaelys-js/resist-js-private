/**
 * Lens manifest for the Carousel component (display category) —
 * scrollable content carousel with navigation controls. Tagged
 * for carousel / slider / gallery / images lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['carousel', 'slider', 'gallery', 'images'],
  description: 'Scrollable content carousel with navigation controls.',
};
