/**
 * Lens manifest for the MatrixRain component (animation
 * category) — Matrix cascading-character backdrop. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['matrix', 'rain', 'code'],
  description: 'Matrix-style falling code rain.',
};
