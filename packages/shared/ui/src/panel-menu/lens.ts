/**
 * Lens manifest for the PanelMenu component (navigation
 * category) — sidebar nested panel menu. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['panel-menu', 'accordion-nav', 'nested', 'sidebar'],
  description: 'PanelMenu component — awaiting implementation.',
};
