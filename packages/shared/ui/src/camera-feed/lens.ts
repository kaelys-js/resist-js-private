/**
 * Lens manifest for the CameraFeed component (iot category) —
 * live camera feed player for streaming video sources. Tagged
 * for camera / feed / live / stream lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'iot',
  tags: ['camera', 'feed', 'live', 'stream'],
  description: 'Live camera feed player.',
};
