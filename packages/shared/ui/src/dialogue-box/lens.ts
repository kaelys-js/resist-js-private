/**
 * Lens manifest for the DialogueBox component (gaming category)
 * — NPC dialogue text box. Tagged for dialogue / box / text /
 * rpg lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['dialogue', 'box', 'text', 'rpg'],
  description: 'NPC dialogue text box.',
};
