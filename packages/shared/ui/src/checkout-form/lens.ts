/**
 * Lens manifest for the CheckoutForm component (commerce
 * category) — multi-step e-commerce checkout form with payment
 * collection. Tagged for checkout / form / multi-step / payment
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['checkout', 'form', 'multi-step', 'payment'],
  description: 'CheckoutForm component — awaiting implementation.',
};
