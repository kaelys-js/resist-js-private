/**
 * Lens manifest for the BattleMenu component (gaming category) —
 * turn-based RPG combat action menu. Tagged for battle / menu /
 * combat / rpg lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['battle', 'menu', 'combat', 'rpg'],
  description: 'Turn-based battle action menu.',
};
