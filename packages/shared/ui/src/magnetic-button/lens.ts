/**
 * Lens manifest for the MagneticButton component (animation
 * category) — cursor-attracting button. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['magnetic', 'hover', 'cursor', 'interactive'],
  description: 'A button that responds to cursor proximity with magnetic pull/push effects.',
};
