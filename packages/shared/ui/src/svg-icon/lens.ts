/**
 * Lens manifest for the SvgIcon component (media category)
 * — generic inline SVG icon. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['svg', 'icon', 'vector'],
  description: 'SVG icon with size/color props.',
};
