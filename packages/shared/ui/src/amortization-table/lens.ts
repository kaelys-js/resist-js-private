/**
 * Lens manifest for the AmortizationTable component (finance
 * category) — tabular loan amortization schedule. Tagged for
 * amortization / table / loan lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['amortization', 'table', 'loan'],
  description: 'Loan amortization schedule.',
};
