/**
 * Lens manifest for the ScrollVelocity component (animation
 * category) — speed-based scroll parallax. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['scroll', 'velocity', 'speed-based', 'parallax'],
  description: 'ScrollVelocity component — awaiting implementation.',
};
