/**
 * Lens manifest for the BillingHistory component (commerce
 * category) — tabular payment / billing history view. Tagged for
 * billing / history / payments lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['billing', 'history', 'payments'],
  description: 'Payment history table.',
};
