/**
 * Lens manifest for the BundleAnalyzer component (devtools
 * category) — treemap visualization of bundle module sizes.
 * Tagged for bundle / analyzer / treemap lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['bundle', 'analyzer', 'treemap'],
  description: 'Bundle size treemap.',
};
