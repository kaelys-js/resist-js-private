/**
 * Lens manifest for the ContactForm component (marketing
 * category) — contact / inquiry submission form. Tagged for
 * contact / form / email / message lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['contact', 'form', 'email', 'message'],
  description: 'ContactForm component — awaiting implementation.',
};
