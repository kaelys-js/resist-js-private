/**
 * Lens manifest for the LiveIndicator component (feedback
 * category) — animated LIVE badge. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['live', 'recording', 'pulse'],
  description: 'Live/recording status pulse.',
};
