/**
 * Lens manifest for the ResetPasswordForm component (form
 * category) — new-password entry form. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['password', 'reset', 'auth', 'form'],
  description: 'New password entry form.',
};
