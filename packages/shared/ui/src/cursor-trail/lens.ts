/**
 * Lens manifest for the CursorTrail component (animation
 * category) — mouse cursor trailing effect. Tagged for cursor /
 * trail / mouse lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['cursor', 'trail', 'mouse'],
  description: 'Mouse cursor trailing effect.',
};
