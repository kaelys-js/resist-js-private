import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['badge', 'label', 'status', 'tag', 'indicator', 'dot', 'tv-variant'],
  description:
    'Small inline label for status indicators, counts, or categories with dot, removable, and icon support.',
};
