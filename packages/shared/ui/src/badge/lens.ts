/**
 * Lens manifest for the Badge component (display category) —
 * small inline label for status indicators, counts, or categories
 * with dot, removable, and icon support. Tagged for badge / label
 * / status / tag / indicator / dot / tv-variant lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['badge', 'label', 'status', 'tag', 'indicator', 'dot', 'tv-variant'],
  description:
    'Small inline label for status indicators, counts, or categories with dot, removable, and icon support.',
};
