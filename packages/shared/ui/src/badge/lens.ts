import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
	category: 'display',
	tags: ['shadcn', 'tv-variant'],
	description: 'Small status indicator label with style variants.',
};
