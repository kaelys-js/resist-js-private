import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['inline', 'alert', 'contextual'],
  description: 'Inline contextual alert message.',
};
