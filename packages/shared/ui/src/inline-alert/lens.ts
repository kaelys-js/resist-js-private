/**
 * Lens manifest for the InlineAlert component (feedback
 * category) — inline contextual alert message. Tagged for
 * inline / alert / contextual lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['inline', 'alert', 'contextual'],
  description: 'Inline contextual alert message.',
};
