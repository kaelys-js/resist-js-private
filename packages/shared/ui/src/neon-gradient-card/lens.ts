import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['neon', 'gradient', 'card', 'glow'],
  description: 'NeonGradientCard component — awaiting implementation.',
};
