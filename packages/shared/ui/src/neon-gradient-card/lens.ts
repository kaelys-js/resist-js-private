/**
 * Lens manifest for the NeonGradientCard component (animation
 * category) — neon-gradient outline card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['neon', 'gradient', 'card', 'glow'],
  description: 'NeonGradientCard component — awaiting implementation.',
};
