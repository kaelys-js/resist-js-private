/**
 * Lens manifest for the DatePicker component (date-time
 * category) — text input + calendar dropdown date picker.
 * Tagged for date / picker / calendar / dropdown lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['date', 'picker', 'calendar', 'dropdown'],
  description: 'A date selection component combining a text input with a calendar dropdown.',
};
