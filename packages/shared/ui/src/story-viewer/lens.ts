/**
 * Lens manifest for the StoryViewer component (social
 * category) — fullscreen tappable story viewer. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['story', 'viewer', 'fullscreen', 'tappable'],
  description: 'StoryViewer component — awaiting implementation.',
};
