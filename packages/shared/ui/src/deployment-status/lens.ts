/**
 * Lens manifest for the DeploymentStatus component (admin
 * category) — deployment pipeline status. Tagged for deployment
 * / pipeline / status lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['deployment', 'pipeline', 'status'],
  description: 'Deployment pipeline status.',
};
