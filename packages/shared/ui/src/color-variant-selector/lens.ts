/**
 * Lens manifest for the ColorVariantSelector component (commerce
 * category) — product color-variant picker with swatches. Tagged
 * for color / variant / selector / swatch lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['color', 'variant', 'selector', 'swatch'],
  description: 'Product color variant picker with swatches.',
};
