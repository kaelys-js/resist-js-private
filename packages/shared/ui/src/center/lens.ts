import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['center', 'alignment', 'flex-center', 'utility'],
  description: 'Center component — awaiting implementation.',
};
