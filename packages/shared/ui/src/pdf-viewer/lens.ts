/**
 * Lens manifest for the PdfViewer component (media category)
 * — embedded PDF viewer. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['pdf', 'viewer', 'document', 'embedded'],
  description: 'PdfViewer component — awaiting implementation.',
};
