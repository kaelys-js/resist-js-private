/**
 * Lens manifest for the JobQueueViewer component (admin
 * category) — background job queue display. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['job', 'queue', 'background'],
  description: 'Background job queue display.',
};
