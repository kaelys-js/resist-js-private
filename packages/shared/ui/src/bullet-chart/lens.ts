import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['bullet', 'chart', 'performance', 'compact'],
  description: 'BulletChart component — awaiting implementation.',
};
