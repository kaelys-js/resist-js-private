/**
 * Lens manifest for the HealthBar component (gaming category)
 * — character HP bar. Tagged for health / bar / hp / game
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['health', 'bar', 'hp', 'game'],
  description: 'Character health points bar.',
};
