/**
 * Lens manifest for the IntersectionObserver component
 * (utility category) — viewport intersection detection. Tagged
 * for intersection / observer / visibility lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['intersection', 'observer', 'visibility'],
  description: 'Viewport intersection detection.',
};
