/**
 * Lens manifest for the List component (data-display
 * category) — generic list container. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['list', 'items', 'collection', 'ordered'],
  description:
    'A component for rendering ordered or unordered lists of data with consistent styling and optional actions.',
};
