/**
 * Lens manifest for the EventCard component (scheduling
 * category) — event listing card with date, title, and
 * location. Tagged for event / card / calendar / display
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['event', 'card', 'calendar', 'display'],
  description: 'EventCard component — awaiting implementation.',
};
