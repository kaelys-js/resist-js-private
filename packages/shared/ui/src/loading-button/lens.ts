/**
 * Lens manifest for the LoadingButton component (form
 * category) — button with built-in spinner state. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['button', 'loading', 'spinner', 'async-action'],
  description: 'LoadingButton component — awaiting implementation.',
};
