/**
 * Lens manifest for the DashboardGrid component (admin
 * category) — draggable / resizable widget grid for dashboards.
 * Tagged for dashboard / grid / widgets / draggable lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['dashboard', 'grid', 'widgets', 'draggable'],
  description: 'DashboardGrid component — awaiting implementation.',
};
