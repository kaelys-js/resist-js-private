/**
 * Lens manifest for the ForceDirectedGraph component
 * (data-display category) — physics-based network graph.
 * Tagged for force / graph / network lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['force', 'graph', 'network'],
  description: 'Physics-based network graph.',
};
