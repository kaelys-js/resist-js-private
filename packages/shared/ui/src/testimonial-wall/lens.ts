/**
 * Lens manifest for the TestimonialWall component (marketing
 * category) — masonry testimonial grid. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['testimonial', 'wall', 'masonry'],
  description: 'Masonry testimonial grid.',
};
