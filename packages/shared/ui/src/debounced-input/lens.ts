import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['debounce', 'input', 'delay'],
  description: 'Input with configurable debounce delay.',
};
