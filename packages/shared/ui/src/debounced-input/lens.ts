/**
 * Lens manifest for the DebouncedInput component (form
 * category) — input with configurable debounce delay. Tagged
 * for debounce / input / delay lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['debounce', 'input', 'delay'],
  description: 'Input with configurable debounce delay.',
};
