import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['in-app', 'message', 'notification'],
  description: 'In-app notification message overlay.',
};
