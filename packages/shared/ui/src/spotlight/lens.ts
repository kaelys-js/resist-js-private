/**
 * Lens manifest for the Spotlight component (animation
 * category) — cursor-tracking radial gradient. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['spotlight', 'hover', 'glow', 'cursor'],
  description:
    'A mouse-tracking radial gradient that follows the cursor, highlighting content areas.',
};
