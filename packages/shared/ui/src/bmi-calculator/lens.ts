/**
 * Lens manifest for the BmiCalculator component (healthcare
 * category) — body-mass-index calculator. Tagged for bmi /
 * calculator / health lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['bmi', 'calculator', 'health'],
  description: 'Body mass index calculator.',
};
