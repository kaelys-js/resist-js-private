/**
 * Lens manifest for the Spoiler component (disclosure
 * category) — collapsible show-more wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'disclosure',
  tags: ['spoiler', 'show-more', 'expandable', 'truncate'],
  description: 'Spoiler component — awaiting implementation.',
};
