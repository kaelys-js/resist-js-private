/**
 * Lens manifest for the FormatNumber component (data-display
 * category) — locale-aware number formatter. Tagged for format
 * / number / locale / display lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['format', 'number', 'locale', 'display'],
  description: 'FormatNumber component — awaiting implementation.',
};
