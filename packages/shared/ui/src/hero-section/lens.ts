/**
 * Lens manifest for the HeroSection component (marketing
 * category) — full-width hero section with CTA. Tagged for
 * hero / section / full-width / cta lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['hero', 'section', 'full-width', 'cta'],
  description: 'HeroSection component — awaiting implementation.',
};
