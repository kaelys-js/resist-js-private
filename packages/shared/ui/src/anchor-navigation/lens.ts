/**
 * Lens manifest for the AnchorNavigation component (navigation
 * category) — in-page nav that highlights the currently visible
 * section and provides click-to-scroll anchor links. Tagged for
 * anchor / scroll / navigation / section lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['anchor', 'scroll', 'navigation', 'section'],
  description:
    'An in-page navigation component that highlights the currently visible section and provides click-to-scroll links.',
};
