/**
 * Lens manifest for the GlareCard component (animation
 * category) — interactive shine-and-tilt card. Tagged for
 * glare / card / shine / tilt lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['glare', 'card', 'shine', 'tilt'],
  description: 'GlareCard component — awaiting implementation.',
};
