import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['glare', 'card', 'shine', 'tilt'],
  description: 'GlareCard component — awaiting implementation.',
};
