import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['swipeable', 'card', 'dismiss', 'tinder-style'],
  description: 'SwipeableCard component — awaiting implementation.',
};
