/**
 * Lens manifest for the RetentionChart component
 * (data-display category) — user retention cohort chart.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['retention', 'cohort', 'chart'],
  description: 'User retention cohort chart.',
};
