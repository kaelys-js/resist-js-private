/**
 * Lens manifest for the TieredMenu component (navigation
 * category) — cascading nested submenu. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['tiered-menu', 'nested', 'sub-menu', 'cascading'],
  description: 'TieredMenu component — awaiting implementation.',
};
