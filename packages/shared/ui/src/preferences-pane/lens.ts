/**
 * Lens manifest for the PreferencesPane component (admin
 * category) — settings preferences panel. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['preferences', 'pane', 'macos', 'settings'],
  description: 'PreferencesPane component — awaiting implementation.',
};
