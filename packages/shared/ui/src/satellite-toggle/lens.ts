/**
 * Lens manifest for the SatelliteToggle component (maps
 * category) — satellite/terrain layer toggle. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['satellite', 'toggle', 'map', 'layer'],
  description: 'Map satellite/terrain layer toggle.',
};
