/**
 * Lens manifest for the Navbar component (navigation
 * category) — top-of-page navigation bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['navbar', 'header', 'navigation', 'responsive'],
  description:
    'A top-level navigation bar, typically containing a brand logo, navigation links, and utility actions.',
};
