import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['cursor', 'smooth', 'follow', 'delayed'],
  description: 'SmoothCursor component — awaiting implementation.',
};
