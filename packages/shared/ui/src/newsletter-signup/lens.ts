import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['newsletter', 'signup', 'email', 'capture'],
  description: 'NewsletterSignup component — awaiting implementation.',
};
