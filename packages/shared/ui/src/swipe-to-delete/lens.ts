/**
 * Lens manifest for the SwipeToDelete component (mobile
 * category) — swipe-to-delete list row. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'mobile',
  tags: ['swipe', 'delete', 'gesture', 'mobile'],
  description: 'Swipe-to-delete list row.',
};
