import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['bookmark', 'save', 'button', 'toggle'],
  description: 'Toggleable bookmark/save button.',
};
