/**
 * Lens manifest for the BookmarkButton component (form category)
 * — toggleable bookmark / save button with filled and outline
 * states. Tagged for bookmark / save / button / toggle lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['bookmark', 'save', 'button', 'toggle'],
  description: 'Toggleable bookmark/save button.',
};
