/**
 * Lens manifest for the LessonProgress component (education
 * category) — student lesson progress meter. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['lesson', 'progress', 'tracker'],
  description: 'Lesson completion tracker.',
};
