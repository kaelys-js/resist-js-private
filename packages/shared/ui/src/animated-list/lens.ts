import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['animated', 'list', 'transition', 'stagger'],
  description:
    'A list component where items animate in sequentially with staggered entrance effects.',
};
