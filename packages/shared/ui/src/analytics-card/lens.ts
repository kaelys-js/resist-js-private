/**
 * Lens manifest for the AnalyticsCard component (data-display
 * category) — single-metric card with trend indicator. Tagged
 * for analytics / metric / trend lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['analytics', 'metric', 'trend'],
  description: 'Analytics metric with trend.',
};
