import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['image', 'crop', 'resize', 'editor'],
  description: 'ImageCropper component — awaiting implementation.',
};
