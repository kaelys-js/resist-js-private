/**
 * Lens manifest for the BulkEdit component (form category) —
 * batch editing interface for multiple records at once. Tagged
 * for bulk / edit / batch / form lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['bulk', 'edit', 'batch', 'form'],
  description: 'Bulk record editing interface.',
};
