import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['sortable', 'drag', 'reorder'],
  description: 'Drag-to-reorder sortable container.',
};
