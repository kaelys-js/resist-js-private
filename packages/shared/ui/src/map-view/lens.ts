/**
 * Lens manifest for the MapView component (maps category) —
 * interactive pan / zoom map canvas. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['map', 'view', 'interactive', 'leaflet'],
  description: 'MapView component — awaiting implementation.',
};
