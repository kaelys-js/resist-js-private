/**
 * Lens manifest for the PeekPreview component (overlay
 * category) — content peek popover. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['peek', 'preview', 'long-press'],
  description: 'Long-press content preview.',
};
