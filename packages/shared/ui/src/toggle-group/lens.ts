import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
	category: 'form',
	tags: ['toggle', 'group', 'select', 'buttons'],
	description: 'Group of toggle buttons with single or multiple selection.',
};
