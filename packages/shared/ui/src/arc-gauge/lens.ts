/**
 * Lens manifest for the ArcGauge component (data-display
 * category) — semicircular arc gauge for displaying a single
 * value within a range. Tagged for arc / gauge / value /
 * indicator lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['arc', 'gauge', 'value', 'indicator'],
  description: 'ArcGauge component — awaiting implementation.',
};
