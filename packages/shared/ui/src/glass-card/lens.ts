/**
 * Lens manifest for the GlassCard component (animation
 * category) — frosted-glass / glassmorphism card. Tagged for
 * glass / card / glassmorphism / frosted lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['glass', 'card', 'glassmorphism', 'frosted'],
  description: 'GlassCard component — awaiting implementation.',
};
