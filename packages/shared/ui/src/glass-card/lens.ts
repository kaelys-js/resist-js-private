import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['glass', 'card', 'glassmorphism', 'frosted'],
  description: 'GlassCard component — awaiting implementation.',
};
