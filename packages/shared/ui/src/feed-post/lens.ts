/**
 * Lens manifest for the FeedPost component (social category) —
 * social-feed post card with author, body, and engagement
 * actions. Tagged for feed / post / social / actions lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['feed', 'post', 'social', 'actions'],
  description: 'FeedPost component — awaiting implementation.',
};
