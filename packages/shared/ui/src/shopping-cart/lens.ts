/**
 * Lens manifest for the ShoppingCart component (commerce
 * category) — cart line-items + totals surface. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['shopping', 'cart', 'items', 'totals'],
  description: 'ShoppingCart component — awaiting implementation.',
};
