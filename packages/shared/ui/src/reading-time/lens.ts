/**
 * Lens manifest for the ReadingTime component (content
 * category) — article reading-time badge. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['reading', 'time', 'estimate'],
  description: 'Estimated reading time badge.',
};
