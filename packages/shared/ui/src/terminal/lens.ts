/**
 * Lens manifest for the Terminal component (typography
 * category) — CLI output display surface. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['terminal', 'console', 'code', 'cli'],
  description:
    'A command-line terminal display component for showing code execution or CLI output.',
};
