/**
 * Lens manifest for the AnnouncementBar component (marketing
 * category) — dismissible top-of-page banner for promos /
 * notices. Tagged for announcement / banner / top-bar /
 * dismissible lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['announcement', 'banner', 'top-bar', 'dismissible'],
  description: 'AnnouncementBar component — awaiting implementation.',
};
