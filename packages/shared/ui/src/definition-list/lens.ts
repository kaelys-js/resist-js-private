/**
 * Lens manifest for the DefinitionList component (data-display
 * category) — term-description pairs display. Tagged for
 * definition / list / dl / dt / dd lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['definition', 'list', 'dl', 'dt', 'dd'],
  description: 'Term-description pairs display.',
};
