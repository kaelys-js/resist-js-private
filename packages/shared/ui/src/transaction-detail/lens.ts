import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['transaction', 'detail', 'finance'],
  description: 'Transaction detail breakdown.',
};
