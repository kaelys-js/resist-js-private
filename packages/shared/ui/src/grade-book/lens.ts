/**
 * Lens manifest for the GradeBook component (education
 * category) — grade / score record table. Tagged for grade /
 * book / score lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['grade', 'book', 'score'],
  description: 'Grade/score record table.',
};
