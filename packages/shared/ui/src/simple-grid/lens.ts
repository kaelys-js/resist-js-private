import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['grid', 'simple', 'auto-columns', 'responsive'],
  description: 'SimpleGrid component — awaiting implementation.',
};
