/**
 * Lens manifest for the IconButton component (form category)
 * — compact icon-only button. Tagged for button / icon /
 * compact / action lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['button', 'icon', 'compact', 'action'],
  description: 'A button that contains only an icon (no text), with accessible labeling.',
};
