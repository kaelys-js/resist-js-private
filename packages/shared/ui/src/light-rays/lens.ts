import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['light-rays', 'volumetric', 'glow', 'radial'],
  description: 'LightRays component — awaiting implementation.',
};
