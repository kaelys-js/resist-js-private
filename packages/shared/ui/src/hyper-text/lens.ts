/**
 * Lens manifest for the HyperText component (animation
 * category) — Matrix-style scramble / decode text effect.
 * Tagged for hyper-text / scramble / decode / matrix lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['hyper-text', 'scramble', 'decode', 'matrix'],
  description: 'HyperText component — awaiting implementation.',
};
