import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['select', 'native', 'dropdown', 'html'],
  description: 'Native HTML select element with consistent styling and option groups.',
};
