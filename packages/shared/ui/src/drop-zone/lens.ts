/**
 * Lens manifest for the DropZone component (form category) —
 * file drag-and-drop upload area. Tagged for drop-zone /
 * drag-drop / file / upload-area lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['drop-zone', 'drag-drop', 'file', 'upload-area'],
  description: 'DropZone component — awaiting implementation.',
};
