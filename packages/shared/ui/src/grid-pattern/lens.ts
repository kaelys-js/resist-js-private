/**
 * Lens manifest for the GridPattern component (animation
 * category) — SVG grid background pattern. Tagged for grid /
 * pattern / background / svg lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['grid', 'pattern', 'background', 'svg'],
  description: 'GridPattern component — awaiting implementation.',
};
