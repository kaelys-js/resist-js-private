/**
 * Lens manifest for the MediaGrid component (media category)
 * — responsive media tile grid. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['media', 'grid', 'gallery'],
  description: 'Responsive media thumbnail grid.',
};
