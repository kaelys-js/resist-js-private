/**
 * Lens manifest for the BarChart component (data-display
 * category) — vertical or horizontal bar chart for categorical
 * data. Tagged for bar / chart / vertical / horizontal lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['bar', 'chart', 'vertical', 'horizontal'],
  description: 'BarChart component — awaiting implementation.',
};
