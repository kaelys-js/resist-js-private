/**
 * Lens manifest for the PricingSlider component (marketing
 * category) — usage-based pricing slider. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['pricing', 'slider', 'usage'],
  description: 'Usage-based pricing calculator slider.',
};
