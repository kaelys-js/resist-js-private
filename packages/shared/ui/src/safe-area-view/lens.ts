/**
 * Lens manifest for the SafeAreaView component (mobile
 * category) — iOS safe-area inset wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'mobile',
  tags: ['safe-area', 'ios', 'inset'],
  description: 'iOS safe area inset wrapper.',
};
