/**
 * Lens manifest for the EmailTemplate component (marketing
 * category) — visual editor / viewer for email templates. Tagged
 * for email / template / editor lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['email', 'template', 'editor'],
  description: 'Email template editor/viewer.',
};
