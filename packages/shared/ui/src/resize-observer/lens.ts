/**
 * Lens manifest for the ResizeObserver component (utility
 * category) — element resize detection wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['resize', 'observer', 'element'],
  description: 'Element resize detection wrapper.',
};
