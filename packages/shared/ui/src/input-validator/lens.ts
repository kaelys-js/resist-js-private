import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['validator', 'input', 'validation', 'rules'],
  description: 'InputValidator component — awaiting implementation.',
};
