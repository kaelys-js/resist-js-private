/**
 * Lens manifest for the InputValidator component (form
 * category) — input wrapper applying validation rules. Tagged
 * for validator / input / validation / rules lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['validator', 'input', 'validation', 'rules'],
  description: 'InputValidator component — awaiting implementation.',
};
