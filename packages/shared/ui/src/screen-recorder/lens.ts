/**
 * Lens manifest for the ScreenRecorder component (media
 * category) — browser screen-capture recorder. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['screen', 'recorder', 'capture'],
  description: 'Browser screen recording capture.',
};
