/**
 * Lens manifest for the DownloadButton component (form
 * category) — file download button with progress feedback.
 * Tagged for download / button / file lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['download', 'button', 'file'],
  description: 'File download button with progress.',
};
