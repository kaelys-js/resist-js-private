/**
 * Lens manifest for the Searchbox component (form category)
 * — search input with clear button and suggestions. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['search', 'input', 'filter', 'query'],
  description: 'A text input optimized for search with clear button and optional suggestions.',
};
