import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['week', 'view', 'calendar', 'grid'],
  description: 'WeekView component — awaiting implementation.',
};
