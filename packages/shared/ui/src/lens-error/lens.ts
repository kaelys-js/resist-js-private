/**
 * Lens manifest for the LensError infrastructure component —
 * error fallback for failed example renders. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'lens',
  tags: ['error', 'boundary', 'fallback', 'documentation'],
  description: 'Error boundary display for Lens documentation system failures.',
};
