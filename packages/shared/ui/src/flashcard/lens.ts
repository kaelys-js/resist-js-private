/**
 * Lens manifest for the Flashcard component (education
 * category) — flippable study flashcard. Tagged for flashcard /
 * study / flip lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['flashcard', 'study', 'flip'],
  description: 'Flippable study flashcard.',
};
