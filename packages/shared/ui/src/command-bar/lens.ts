/**
 * Lens manifest for the CommandBar component (navigation
 * category) — horizontal command / action bar. Tagged for
 * command / bar / actions lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['command', 'bar', 'actions'],
  description: 'Horizontal command/action bar.',
};
