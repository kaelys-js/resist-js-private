import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['command', 'bar', 'actions'],
  description: 'Horizontal command/action bar.',
};
