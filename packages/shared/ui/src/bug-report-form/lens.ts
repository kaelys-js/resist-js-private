/**
 * Lens manifest for the BugReportForm component (form category)
 * — submission form for user-reported bugs. Tagged for bug /
 * report / form / feedback lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['bug', 'report', 'form', 'feedback'],
  description: 'Bug report submission form.',
};
