/**
 * Lens manifest for the Pagination compound component
 * (navigation category) — paginated list / table navigation.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['pagination', 'pages', 'navigation', 'numbered'],
  description: 'Page navigation with previous, next, and numbered page buttons.',
};
