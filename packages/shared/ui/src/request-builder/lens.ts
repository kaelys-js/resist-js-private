/**
 * Lens manifest for the RequestBuilder component (devtools
 * category) — HTTP request builder form. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['request', 'builder', 'http', 'api'],
  description: 'HTTP request builder form.',
};
