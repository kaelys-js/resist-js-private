/**
 * Lens manifest for the Stack component (layout category) —
 * single-axis flex layout primitive. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['stack', 'layout', 'vertical', 'horizontal'],
  description:
    'A layout component for arranging children with consistent spacing along a single axis.',
};
