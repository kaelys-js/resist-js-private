import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['import', 'wizard', 'multi-step', 'data'],
  description: 'ImportWizard component — awaiting implementation.',
};
