/**
 * Lens manifest for the ImportWizard component (admin
 * category) — multi-step bulk-import wizard. Tagged for import
 * / wizard / multi-step / data lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['import', 'wizard', 'multi-step', 'data'],
  description: 'ImportWizard component — awaiting implementation.',
};
