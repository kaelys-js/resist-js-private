/**
 * Lens manifest for the EmailPreview component (overlay
 * category) — preview modal showing rendered email template
 * output. Tagged for email / preview / template lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['email', 'preview', 'template'],
  description: 'Email template preview modal.',
};
