/**
 * Lens manifest for the CascadeSelect component (form category)
 * — multi-level dropdown for selecting values from hierarchical
 * data structures. Tagged for select / cascading / hierarchical /
 * multi-level lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['select', 'cascading', 'hierarchical', 'multi-level'],
  description: 'A multi-level dropdown for selecting values from hierarchical data structures.',
};
