/**
 * Lens manifest for the ComboCounter component (gaming category)
 * — hit combo counter display. Tagged for combo / counter / hit
 * / game lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['combo', 'counter', 'hit', 'game'],
  description: 'Hit combo counter display.',
};
