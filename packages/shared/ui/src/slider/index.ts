/**
 * Barrel re-export for the slider component — exposes the
 * Slider Svelte component (Bits UI `Slider` wrapper) under
 * stable public names.
 *
 * @module
 */

import Root from './slider.svelte';

export { Root, Root as Slider };
