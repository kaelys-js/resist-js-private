/**
 * Lens manifest for the Slider component (form category) —
 * range slider with track and draggable thumb. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['slider', 'range', 'input', 'number'],
  description: 'Range slider with track, filled range, and draggable thumb.',
};
