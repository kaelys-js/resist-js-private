/**
 * Lens manifest for the GroupedBarChart component
 * (data-display category) — side-by-side grouped bar chart.
 * Tagged for grouped / bar / chart lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['grouped', 'bar', 'chart'],
  description: 'Side-by-side grouped bar chart.',
};
