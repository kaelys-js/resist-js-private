/**
 * Lens manifest for the AppSidebar component (navigation category)
 * — main application sidebar surfacing project navigation. Tagged
 * for sidebar / app-shell / navigation / panel lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['sidebar', 'app-shell', 'navigation', 'panel'],
  description: 'Main application sidebar with project navigation.',
};
