import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['level', 'indicator', 'rank', 'tier'],
  description: 'LevelIndicator component — awaiting implementation.',
};
