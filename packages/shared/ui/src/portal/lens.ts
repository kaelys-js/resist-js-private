/**
 * Lens manifest for the Portal component (utility category)
 * — generic portal wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['portal', 'teleport', 'dom'],
  description: 'Render children into a DOM subtree.',
};
