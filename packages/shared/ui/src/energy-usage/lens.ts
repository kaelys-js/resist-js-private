/**
 * Lens manifest for the EnergyUsage component (iot category) —
 * energy / power consumption dashboard widget. Tagged for energy
 * / usage / consumption / iot lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'iot',
  tags: ['energy', 'usage', 'consumption', 'iot'],
  description: 'Energy consumption display.',
};
