/**
 * Lens manifest for the CreditCardInput component (commerce
 * category) — formatted credit-card number / expiry / cvc input.
 * Tagged for credit-card / input / formatted / number lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['credit-card', 'input', 'formatted', 'number'],
  description: 'CreditCardInput component — awaiting implementation.',
};
