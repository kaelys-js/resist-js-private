import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['credit-card', 'input', 'formatted', 'number'],
  description: 'CreditCardInput component — awaiting implementation.',
};
