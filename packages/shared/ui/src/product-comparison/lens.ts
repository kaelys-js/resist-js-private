/**
 * Lens manifest for the ProductComparison component
 * (commerce category) — product feature comparison.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['product', 'comparison', 'side-by-side', 'features'],
  description: 'ProductComparison component — awaiting implementation.',
};
