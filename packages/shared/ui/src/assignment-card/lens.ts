import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['assignment', 'card', 'education'],
  description: 'Assignment submission card.',
};
