/**
 * Lens manifest for the TextInput component (form category)
 * — single-line text input field. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['text', 'input', 'field', 'single-line'],
  description: 'TextInput component — awaiting implementation.',
};
