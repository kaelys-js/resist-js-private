/**
 * Lens manifest for the DeviceCard component (iot category) —
 * smart device status card. Tagged for device / card / iot /
 * smart-home lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'iot',
  tags: ['device', 'card', 'iot', 'smart-home'],
  description: 'Smart device status card.',
};
