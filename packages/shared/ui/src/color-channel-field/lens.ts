import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'channel', 'input', 'rgb-hsl'],
  description: 'ColorChannelField component — awaiting implementation.',
};
