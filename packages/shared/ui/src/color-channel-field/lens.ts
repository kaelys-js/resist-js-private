/**
 * Lens manifest for the ColorChannelField component (form
 * category) — number input for editing a single color channel
 * (RGB / HSL). Tagged for color / channel / input / rgb-hsl
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'channel', 'input', 'rgb-hsl'],
  description: 'ColorChannelField component — awaiting implementation.',
};
