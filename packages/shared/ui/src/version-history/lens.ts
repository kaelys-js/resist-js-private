import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['version', 'history', 'revisions', 'document'],
  description: 'VersionHistory component — awaiting implementation.',
};
