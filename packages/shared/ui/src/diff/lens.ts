import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['diff', 'compare', 'before-after', 'changes'],
  description: 'Diff component — awaiting implementation.',
};
