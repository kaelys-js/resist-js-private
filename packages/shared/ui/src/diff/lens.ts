/**
 * Lens manifest for the Diff component (data-display category)
 * — side-by-side or inline text-diff display. Tagged for diff
 * / compare / before-after / changes lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['diff', 'compare', 'before-after', 'changes'],
  description: 'Diff component — awaiting implementation.',
};
