import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['flip', 'card', 'front-back', '3d'],
  description: 'FlipCard component — awaiting implementation.',
};
