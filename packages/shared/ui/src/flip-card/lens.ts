/**
 * Lens manifest for the FlipCard component (animation
 * category) — front / back card with 3D flip transition. Tagged
 * for flip / card / front-back / 3d lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['flip', 'card', 'front-back', '3d'],
  description: 'FlipCard component — awaiting implementation.',
};
