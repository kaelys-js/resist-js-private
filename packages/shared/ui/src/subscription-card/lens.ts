/**
 * Lens manifest for the SubscriptionCard component (commerce
 * category) — active subscription display card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['subscription', 'plan', 'card'],
  description: 'Active subscription display.',
};
