/**
 * Lens manifest for the PanelStack component (navigation
 * category) — drill-down panel stack. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['panel', 'stack', 'navigable', 'nested'],
  description: 'PanelStack component — awaiting implementation.',
};
