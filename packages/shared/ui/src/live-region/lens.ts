import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'a11y',
  tags: ['live-region', 'aria', 'announcer', 'a11y'],
  description: 'LiveRegion component — awaiting implementation.',
};
