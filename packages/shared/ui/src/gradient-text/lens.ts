/**
 * Lens manifest for the GradientText component (animation
 * category) — gradient-filled text. Tagged for gradient / text
 * / css / colorful lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['gradient', 'text', 'css', 'colorful'],
  description: 'GradientText component — awaiting implementation.',
};
