/**
 * Lens manifest for the CandlestickChart component (data-display
 * category) — financial OHLC candlestick chart. Tagged for
 * candlestick / chart / ohlc / financial lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['candlestick', 'chart', 'ohlc', 'financial'],
  description: 'CandlestickChart component — awaiting implementation.',
};
