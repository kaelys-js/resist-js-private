/**
 * Lens manifest for the PatientCard component (healthcare
 * category) — patient vitals card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['patient', 'card', 'medical'],
  description: 'Patient summary card.',
};
