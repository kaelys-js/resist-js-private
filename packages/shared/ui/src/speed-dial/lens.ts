import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['speed-dial', 'fab', 'actions', 'floating'],
  description: 'A floating action button that expands to reveal a set of related actions.',
};
