/**
 * Lens manifest for the TrialBanner component (marketing
 * category) — trial countdown banner. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['trial', 'banner', 'countdown'],
  description: 'Trial period countdown banner.',
};
