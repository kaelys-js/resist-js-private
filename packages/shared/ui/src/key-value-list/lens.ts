/**
 * Lens manifest for the KeyValueList component (data-display
 * category) — key-value property display. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['key-value', 'property', 'list'],
  description: 'Key-value property display.',
};
