/**
 * Barrel re-export for the skeleton component — exposes the
 * Skeleton Svelte component under stable public names.
 *
 * @module
 */

import Root from './skeleton.svelte';

export {
  Root,
  //
  Root as Skeleton,
};
