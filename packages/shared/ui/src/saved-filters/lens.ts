/**
 * Lens manifest for the SavedFilters component (data-display
 * category) — named filter presets list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['saved', 'filters', 'presets', 'named'],
  description: 'SavedFilters component — awaiting implementation.',
};
