import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['hero', 'parallax', 'scroll', '3d-perspective'],
  description: 'HeroParallax component — awaiting implementation.',
};
