import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['logo', 'branding', 'animated', 'identity'],
  description: 'Animated application logo with sparkle effect.',
};
