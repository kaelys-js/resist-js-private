/**
 * Lens manifest for the AppLogo component (display category) —
 * animated application logo with a sparkle effect for branding.
 * Tagged for logo / branding / animated / identity lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['logo', 'branding', 'animated', 'identity'],
  description: 'Animated application logo with sparkle effect.',
};
