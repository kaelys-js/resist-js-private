/**
 * Lens manifest for the CodeBlock component (typography category)
 * — syntax-highlighted code display powered by Shiki. Tagged for
 * code / syntax-highlighting / pre / shiki lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['code', 'syntax-highlighting', 'pre', 'shiki'],
  description: 'Syntax-highlighted code display powered by Shiki.',
};
