/**
 * Lens manifest for the CohortTable component (data-display
 * category) — cohort analysis matrix table. Tagged for cohort /
 * table / matrix lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['cohort', 'table', 'matrix'],
  description: 'Cohort analysis matrix table.',
};
