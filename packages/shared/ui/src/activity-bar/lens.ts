/**
 * Lens manifest for the ActivityBar component (navigation
 * category) — VS Code-style vertical icon sidebar. Tagged for
 * activity / bar / sidebar / icons lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['activity', 'bar', 'sidebar', 'icons'],
  description: 'VS Code-style icon sidebar.',
};
