/**
 * Lens manifest for the ActionSheet component (overlay category)
 * — iOS-style bottom action sheet for mobile UIs. Tagged for
 * action-sheet / bottom-actions / ios / mobile lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['action-sheet', 'bottom-actions', 'ios', 'mobile'],
  description: 'ActionSheet component — awaiting implementation.',
};
