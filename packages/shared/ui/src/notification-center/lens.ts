/**
 * Lens manifest for the NotificationCenter component (overlay
 * category) — drawer panel listing notifications. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['notification', 'center', 'grouped', 'panel'],
  description: 'NotificationCenter component — awaiting implementation.',
};
