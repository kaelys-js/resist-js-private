/**
 * Lens manifest for the ActivityFeed component (social category)
 * — chronological stream of events / activity entries. Tagged for
 * activity / feed / stream / events lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['activity', 'feed', 'stream', 'events'],
  description: 'ActivityFeed component — awaiting implementation.',
};
