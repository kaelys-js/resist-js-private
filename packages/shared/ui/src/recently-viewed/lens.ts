import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['recently', 'viewed', 'history'],
  description: 'Recently viewed items strip.',
};
