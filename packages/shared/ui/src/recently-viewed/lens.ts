/**
 * Lens manifest for the RecentlyViewed component (commerce
 * category) — recently viewed items strip. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['recently', 'viewed', 'history'],
  description: 'Recently viewed items strip.',
};
