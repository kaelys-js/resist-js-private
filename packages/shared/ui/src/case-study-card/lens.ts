/**
 * Lens manifest for the CaseStudyCard component (marketing
 * category) — portfolio / showcase card for case studies. Tagged
 * for case-study / card / portfolio / showcase lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['case-study', 'card', 'portfolio', 'showcase'],
  description: 'CaseStudyCard component — awaiting implementation.',
};
