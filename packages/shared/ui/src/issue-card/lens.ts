/**
 * Lens manifest for the IssueCard component (admin / tracker)
 * — bug / issue tracker card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['issue', 'bug', 'card', 'tracker'],
  description: 'Issue/bug tracker card.',
};
