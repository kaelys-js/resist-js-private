/**
 * Lens manifest for the ProductReviews component (commerce
 * category) — product reviews list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['product', 'reviews', 'list'],
  description: 'Product reviews list.',
};
