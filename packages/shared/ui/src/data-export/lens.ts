/**
 * Lens manifest for the DataExport component (admin category) —
 * multi-format data export download dialog. Tagged for data /
 * export / csv-json / download lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['data', 'export', 'csv-json', 'download'],
  description: 'DataExport component — awaiting implementation.',
};
