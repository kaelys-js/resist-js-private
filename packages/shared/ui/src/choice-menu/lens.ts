/**
 * Lens manifest for the ChoiceMenu component (gaming category)
 * — RPG dialogue choice selection menu. Tagged for choice / menu
 * / dialogue / rpg lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['choice', 'menu', 'dialogue', 'rpg'],
  description: 'Dialogue choice selection menu.',
};
