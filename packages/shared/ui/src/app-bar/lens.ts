/**
 * Lens manifest for the AppBar component (navigation category) —
 * top-of-screen toolbar / header for application chrome. Tagged
 * for app-bar / header / toolbar / top-bar lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['app-bar', 'header', 'toolbar', 'top-bar'],
  description: 'AppBar component — awaiting implementation.',
};
