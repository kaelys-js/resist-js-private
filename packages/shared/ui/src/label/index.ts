/**
 * Barrel re-export for the label component — exposes the
 * Label Svelte component under its stable public name.
 *
 * @module
 */

import Root from './label.svelte';

export {
  Root,
  //
  Root as Label,
};
