import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['button-group', 'group', 'toolbar', 'actions'],
  description: 'Groups related buttons with consistent spacing and optional separators.',
};
