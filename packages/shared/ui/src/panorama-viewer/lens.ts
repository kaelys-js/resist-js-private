/**
 * Lens manifest for the PanoramaViewer component (media
 * category) — 360° panorama viewer. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['panorama', '360', 'viewer'],
  description: '360-degree panoramic image viewer.',
};
