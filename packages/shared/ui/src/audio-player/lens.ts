/**
 * Lens manifest for the AudioPlayer component (media category) —
 * audio playback surface with transport controls. Tagged for
 * audio / player / playback / controls lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['audio', 'player', 'playback', 'controls'],
  description: 'AudioPlayer component — awaiting implementation.',
};
