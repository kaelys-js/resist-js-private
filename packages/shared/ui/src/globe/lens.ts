/**
 * Lens manifest for the Globe component (animation category)
 * — interactive 3D globe with arcs and markers. Tagged for
 * globe / 3d / interactive / visualization lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['globe', '3d', 'interactive', 'visualization'],
  description: 'An interactive 3D globe visualization with animated arcs, markers, and rotation.',
};
