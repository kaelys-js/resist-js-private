import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['combobox', 'search', 'select', 'filterable'],
  description: 'A searchable dropdown that combines a text input with a filterable option list.',
};
