/**
 * Lens manifest for the Combobox component (form category) —
 * searchable dropdown combining a text input with a filterable
 * option list. Tagged for combobox / search / select /
 * filterable lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['combobox', 'search', 'select', 'filterable'],
  description: 'A searchable dropdown that combines a text input with a filterable option list.',
};
