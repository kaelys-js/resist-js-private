/**
 * Lens manifest for the EmbedCard component (display category)
 * — oEmbed-style link preview card. Tagged for embed / card /
 * oembed / link-preview lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['embed', 'card', 'oembed', 'link-preview'],
  description: 'EmbedCard component — awaiting implementation.',
};
