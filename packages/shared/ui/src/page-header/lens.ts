/**
 * Lens manifest for the PageHeader component (layout
 * category) — page-level header. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['header', 'page', 'breadcrumb', 'title'],
  description:
    'A component displaying page title, breadcrumbs, back button, and optional actions at the top of a page.',
};
