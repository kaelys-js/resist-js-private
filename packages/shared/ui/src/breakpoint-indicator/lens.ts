/**
 * Lens manifest for the BreakpointIndicator component (utility
 * category) — dev-mode display of the current Tailwind breakpoint.
 * Tagged for breakpoint / indicator / dev lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['breakpoint', 'indicator', 'dev'],
  description: 'Dev-mode current breakpoint display.',
};
