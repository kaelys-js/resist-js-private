import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['meteor', 'shooting-star', 'particle', 'trail'],
  description: 'MeteorEffect component — awaiting implementation.',
};
