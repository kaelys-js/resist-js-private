/**
 * Lens manifest for the EmojiPicker component (form category)
 * — searchable grid of emoji for inline insertion. Tagged for
 * emoji / picker / searchable / grid lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['emoji', 'picker', 'searchable', 'grid'],
  description: 'EmojiPicker component — awaiting implementation.',
};
