/**
 * Lens manifest for the ManaBar component (gaming category)
 * — RPG mana / energy bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['mana', 'bar', 'mp', 'game'],
  description: 'Character mana/magic bar.',
};
