/**
 * Lens manifest for the InlineEdit component (form category)
 * — click-to-edit text with save / cancel. Tagged for inline /
 * edit / click-to-edit lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['inline', 'edit', 'click-to-edit'],
  description: 'Click-to-edit text with save/cancel.',
};
