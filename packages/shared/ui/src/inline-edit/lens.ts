import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['inline', 'edit', 'click-to-edit'],
  description: 'Click-to-edit text with save/cancel.',
};
