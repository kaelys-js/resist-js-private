/**
 * Lens manifest for the SubscribeButton component (form
 * category) — channel subscribe toggle. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['subscribe', 'button', 'toggle'],
  description: 'Email/channel subscribe toggle button.',
};
