/**
 * Lens manifest for the Captcha component (form category) —
 * bot-prevention challenge wrapper. Tagged for captcha /
 * verification / bot-prevention / challenge lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['captcha', 'verification', 'bot-prevention', 'challenge'],
  description: 'Captcha component — awaiting implementation.',
};
