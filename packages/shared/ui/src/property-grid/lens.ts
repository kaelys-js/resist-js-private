/**
 * Lens manifest for the PropertyGrid component (data-display
 * category) — editable key/value property grid. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['property', 'grid', 'key-value', 'editable'],
  description: 'PropertyGrid component — awaiting implementation.',
};
