import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['canvas', 'reveal', 'dots', 'hover'],
  description: 'CanvasRevealEffect component — awaiting implementation.',
};
