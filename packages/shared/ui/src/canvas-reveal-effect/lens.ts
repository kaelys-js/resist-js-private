/**
 * Lens manifest for the CanvasRevealEffect component (animation
 * category) — canvas-based dot-reveal hover effect. Tagged for
 * canvas / reveal / dots / hover lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['canvas', 'reveal', 'dots', 'hover'],
  description: 'CanvasRevealEffect component — awaiting implementation.',
};
