/**
 * Lens manifest for the PaymentMethodCard component
 * (commerce category) — saved payment method card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['payment', 'method', 'card'],
  description: 'Saved payment method card.',
};
