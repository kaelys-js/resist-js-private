import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['confetti', 'celebration', 'particles', 'effect'],
  description: 'An animated confetti burst or cannon effect triggered by an action or event.',
};
