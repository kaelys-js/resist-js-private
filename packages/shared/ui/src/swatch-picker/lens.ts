/**
 * Lens manifest for the SwatchPicker component (form
 * category) — colour swatch grid picker. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['swatch', 'picker', 'color-grid', 'selectable'],
  description: 'SwatchPicker component — awaiting implementation.',
};
