/**
 * Lens manifest for the Accordion component (disclosure category)
 * — vertically stacked sections that expand and collapse to
 * show/hide content. Tagged for accordion / collapsible / expand
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'disclosure',
  tags: ['accordion', 'collapsible', 'expand', 'sections'],
  description: 'Vertically stacked sections that expand and collapse to show/hide content.',
};
