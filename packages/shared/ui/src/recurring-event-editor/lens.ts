/**
 * Lens manifest for the RecurringEventEditor component
 * (scheduling category) — RRULE recurrence rule builder.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['recurring', 'event', 'editor', 'rrule'],
  description: 'Recurring event rule builder.',
};
