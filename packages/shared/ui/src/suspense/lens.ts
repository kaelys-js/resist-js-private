/**
 * Lens manifest for the Suspense component (utility
 * category) — async loading fallback wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['suspense', 'async', 'fallback'],
  description: 'Async loading fallback wrapper.',
};
