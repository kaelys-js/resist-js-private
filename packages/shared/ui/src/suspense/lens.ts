import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['suspense', 'async', 'fallback'],
  description: 'Async loading fallback wrapper.',
};
