/**
 * Lens manifest for the Rubric component (education
 * category) — grading rubric matrix. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['rubric', 'grading', 'matrix'],
  description: 'Grading rubric matrix.',
};
