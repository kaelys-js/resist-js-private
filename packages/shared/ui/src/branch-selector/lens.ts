/**
 * Lens manifest for the BranchSelector component (navigation
 * category) — git-style branch picker dropdown. Tagged for branch
 * / git / selector lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['branch', 'git', 'selector'],
  description: 'Git branch picker dropdown.',
};
