/**
 * Lens manifest for the HeroHighlight component (animation
 * category) — hero backdrop with a mouse-following gradient.
 * Tagged for hero / highlight / gradient / mouse-follow lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['hero', 'highlight', 'gradient', 'mouse-follow'],
  description: 'HeroHighlight component — awaiting implementation.',
};
