/**
 * Lens manifest for the Link component (typography category)
 * — styled anchor element. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['link', 'anchor', 'navigation', 'url'],
  description: 'A navigation element for navigating to URLs or triggering actions.',
};
