/**
 * Lens manifest for the Paywall component (commerce
 * category) — content paywall blocker. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['paywall', 'gate', 'upgrade'],
  description: 'Feature-gated paywall overlay.',
};
