/**
 * Lens manifest for the PressMentions component (marketing
 * category) — press logo strip. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['press', 'mentions', 'as-seen-in', 'media'],
  description: 'PressMentions component — awaiting implementation.',
};
