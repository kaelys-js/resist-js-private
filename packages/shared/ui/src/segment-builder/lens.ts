/**
 * Lens manifest for the SegmentBuilder component (marketing
 * category) — audience segment rule builder. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['segment', 'builder', 'audience'],
  description: 'Audience segment builder.',
};
