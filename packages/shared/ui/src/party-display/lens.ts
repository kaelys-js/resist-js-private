/**
 * Lens manifest for the PartyDisplay component (gaming
 * category) — RPG party roster. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['party', 'display', 'members', 'rpg'],
  description: 'Party member status display.',
};
