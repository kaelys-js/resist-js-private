import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['traffic-lights', 'macos', 'window', 'controls'],
  description: 'TrafficLights component — awaiting implementation.',
};
