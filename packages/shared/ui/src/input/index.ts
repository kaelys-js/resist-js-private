/**
 * Barrel re-export for the input component — exposes the
 * Input Svelte component under its stable public name.
 *
 * @module
 */

import Root from './input.svelte';

export {
  Root,
  //
  Root as Input,
};
