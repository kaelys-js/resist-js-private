/**
 * Lens manifest for the Input component (form category) —
 * styled text input field. Tagged for input / text / field /
 * text-input lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['input', 'text', 'field', 'text-input'],
  description: 'Text input field.',
};
