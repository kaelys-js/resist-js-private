import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['tracker', 'status-grid', 'uptime', 'heatmap'],
  description: 'Tracker component — awaiting implementation.',
};
