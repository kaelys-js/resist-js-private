/**
 * Lens manifest for the OrderSummary component (commerce
 * category) — checkout order summary. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['order', 'summary', 'totals', 'tax'],
  description: 'OrderSummary component — awaiting implementation.',
};
