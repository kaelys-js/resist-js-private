/**
 * Lens manifest for the CircularProgress component (feedback
 * category) — ring-style percentage progress indicator. Tagged
 * for progress / circular / ring / percentage lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['progress', 'circular', 'ring', 'percentage'],
  description: 'CircularProgress component — awaiting implementation.',
};
