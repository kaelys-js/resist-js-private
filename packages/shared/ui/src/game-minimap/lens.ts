/**
 * Lens manifest for the GameMinimap component (gaming
 * category) — in-game minimap overlay. Tagged for minimap /
 * map / game / overlay lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['minimap', 'map', 'game', 'overlay'],
  description: 'In-game minimap overlay.',
};
