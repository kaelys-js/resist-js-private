import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['price', 'display', 'currency', 'sale'],
  description: 'PriceDisplay component — awaiting implementation.',
};
