/**
 * Lens manifest for the Spreadsheet component (data-display
 * category) — editable cell grid. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['spreadsheet', 'editable', 'grid', 'cells'],
  description: 'Spreadsheet component — awaiting implementation.',
};
