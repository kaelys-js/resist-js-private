/**
 * Lens manifest for the ColorWheel component (form category) —
 * circular hue picker (HSL color wheel). Tagged for color /
 * wheel / circular / hue-picker lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'wheel', 'circular', 'hue-picker'],
  description: 'ColorWheel component — awaiting implementation.',
};
