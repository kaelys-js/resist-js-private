import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'wheel', 'circular', 'hue-picker'],
  description: 'ColorWheel component — awaiting implementation.',
};
