import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['scatter', 'chart', 'xy-plot', 'dots'],
  description: 'ScatterChart component — awaiting implementation.',
};
