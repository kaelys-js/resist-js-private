/**
 * Lens manifest for the ChipGroup component (form category) —
 * grouped selectable chips for multi-select tag UIs. Tagged for
 * chip / group / selection / container lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['chip', 'group', 'selection', 'container'],
  description: 'ChipGroup component — awaiting implementation.',
};
