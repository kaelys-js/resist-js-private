/**
 * Lens manifest for the TiltCard component (animation
 * category) — mouse-tracking 3D tilt card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['tilt', 'card', '3d', 'mouse'],
  description: 'Mouse-tracking 3D tilt card.',
};
