/**
 * Lens manifest for the QuestLog component (gaming category)
 * — RPG quest tracker list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['quest', 'log', 'tracker', 'rpg'],
  description: 'Quest tracker list.',
};
