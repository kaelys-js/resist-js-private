/**
 * Lens manifest for the AgentStatus component (social category)
 * — agent thinking / busy indicator for chat-style UIs. Tagged
 * for agent / status / thinking / indicator lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['agent', 'status', 'thinking', 'indicator'],
  description: 'AgentStatus component — awaiting implementation.',
};
