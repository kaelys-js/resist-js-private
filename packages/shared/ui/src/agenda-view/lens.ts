/**
 * Lens manifest for the AgendaView component (scheduling
 * category) — chronological list of upcoming events. Tagged for
 * agenda / view / event / list lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['agenda', 'view', 'event', 'list'],
  description: 'AgendaView component — awaiting implementation.',
};
