/**
 * Lens manifest for the LootDrop component (gaming category)
 * — RPG loot reveal panel. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['loot', 'drop', 'item', 'game'],
  description: 'Item drop notification.',
};
