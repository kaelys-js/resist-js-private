import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['burger', 'hamburger', 'menu-toggle', 'mobile-nav'],
  description: 'BurgerMenu component — awaiting implementation.',
};
