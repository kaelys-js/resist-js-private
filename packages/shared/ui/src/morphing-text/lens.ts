/**
 * Lens manifest for the MorphingText component (animation
 * category) — text that morphs between phrases. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['morphing', 'text', 'transition', 'interpolation'],
  description: 'MorphingText component — awaiting implementation.',
};
