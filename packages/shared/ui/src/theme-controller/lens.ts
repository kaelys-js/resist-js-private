/**
 * Lens manifest for the ThemeController component (utility
 * category) — light/dark/system theme toggle. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['theme', 'dark-mode', 'toggle', 'system'],
  description: 'A toggle or selector that switches between light, dark, and custom themes.',
};
