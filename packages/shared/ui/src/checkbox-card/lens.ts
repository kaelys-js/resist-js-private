/**
 * Lens manifest for the CheckboxCard component (form category)
 * — selectable card with embedded checkbox for multi-pick UIs.
 * Tagged for checkbox / card / selectable / multi lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['checkbox', 'card', 'selectable', 'multi'],
  description: 'CheckboxCard component — awaiting implementation.',
};
