/**
 * Lens manifest for the ConsentManager component (legal
 * category) — multi-category consent preferences. Tagged for
 * consent / privacy / preferences lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'legal',
  tags: ['consent', 'privacy', 'preferences'],
  description: 'Multi-category consent preferences.',
};
