import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'legal',
  tags: ['consent', 'privacy', 'preferences'],
  description: 'Multi-category consent preferences.',
};
