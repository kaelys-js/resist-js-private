/**
 * Lens manifest for the QrCode component (media category) —
 * QR code generator/display. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['qr-code', 'barcode', 'generate', 'scan'],
  description: 'Generates and displays a QR code from a string value.',
};
