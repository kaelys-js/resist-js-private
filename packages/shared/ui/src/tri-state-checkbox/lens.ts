/**
 * Lens manifest for the TriStateCheckbox component (form
 * category) — checkbox with indeterminate state. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['checkbox', 'tri-state', 'indeterminate', 'partial'],
  description: 'TriStateCheckbox component — awaiting implementation.',
};
