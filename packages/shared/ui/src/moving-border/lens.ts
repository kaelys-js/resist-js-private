/**
 * Lens manifest for the MovingBorder component (animation
 * category) — animated gradient border. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['moving', 'border', 'gradient', 'rotating'],
  description: 'MovingBorder component — awaiting implementation.',
};
