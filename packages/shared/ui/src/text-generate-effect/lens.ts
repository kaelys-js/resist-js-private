/**
 * Lens manifest for the TextGenerateEffect component
 * (animation category) — word-by-word text reveal. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['text', 'generate', 'word-by-word', 'reveal'],
  description: 'TextGenerateEffect component — awaiting implementation.',
};
