/**
 * Lens manifest for the TypewriterEffect component (animation
 * category) — character-by-character typing animation.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['typewriter', 'typing', 'cursor', 'sequential'],
  description: 'TypewriterEffect component — awaiting implementation.',
};
