import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['2fa', 'totp', 'auth', 'security'],
  description: 'Two-factor authentication code entry.',
};
