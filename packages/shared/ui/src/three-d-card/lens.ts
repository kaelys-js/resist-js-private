/**
 * Lens manifest for the ThreeDCard component (animation
 * category) — 3D perspective tilt card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['3d', 'card', 'perspective'],
  description: 'Full 3D perspective card.',
};
