import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['message', 'bubble', 'chat', 'alignment'],
  description: 'MessageBubble component — awaiting implementation.',
};
