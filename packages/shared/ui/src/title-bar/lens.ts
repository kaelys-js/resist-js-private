/**
 * Lens manifest for the TitleBar component (desktop
 * category) — custom Electron window title bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['title-bar', 'window', 'custom', 'electron'],
  description: 'TitleBar component — awaiting implementation.',
};
