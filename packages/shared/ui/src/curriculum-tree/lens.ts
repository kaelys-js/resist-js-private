/**
 * Lens manifest for the CurriculumTree component (education
 * category) — curriculum hierarchy tree. Tagged for curriculum
 * / tree / education lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['curriculum', 'tree', 'education'],
  description: 'Curriculum hierarchy tree.',
};
