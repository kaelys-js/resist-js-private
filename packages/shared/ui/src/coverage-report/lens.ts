/**
 * Lens manifest for the CoverageReport component (devtools
 * category) — code-coverage visualization. Tagged for coverage
 * / report / code lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['coverage', 'report', 'code'],
  description: 'Code coverage visualization.',
};
