/**
 * Lens manifest for the VoteButton component (form category)
 * — upvote / downvote toggle. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['vote', 'upvote', 'downvote', 'button'],
  description: 'Upvote/downvote toggle button.',
};
