/**
 * Lens manifest for the SurveyForm component (form category)
 * — survey/questionnaire surface. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['survey', 'questionnaire', 'form'],
  description: 'Survey/questionnaire form.',
};
