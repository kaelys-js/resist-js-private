import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['message', 'bar', 'inline', 'notification'],
  description: 'MessageBar component — awaiting implementation.',
};
