import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['textarea', 'autosize', 'auto-grow', 'expanding'],
  description: 'AutosizeTextarea component — awaiting implementation.',
};
