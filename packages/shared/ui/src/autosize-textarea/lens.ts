/**
 * Lens manifest for the AutosizeTextarea component (form
 * category) — textarea that auto-grows as content wraps onto new
 * lines. Tagged for textarea / autosize / auto-grow / expanding
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['textarea', 'autosize', 'auto-grow', 'expanding'],
  description: 'AutosizeTextarea component — awaiting implementation.',
};
