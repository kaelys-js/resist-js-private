import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['notification', 'alert', 'popup', 'push'],
  description:
    'A persistent notification panel or popup with title, description, and action buttons, richer than Toast.',
};
