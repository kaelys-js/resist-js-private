/**
 * Lens manifest for the Hashtag component (social category) —
 * clickable hashtag link. Tagged for hashtag / tag / link
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['hashtag', 'tag', 'link'],
  description: 'Clickable hashtag link.',
};
