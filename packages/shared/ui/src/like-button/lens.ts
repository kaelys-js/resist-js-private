/**
 * Lens manifest for the LikeButton component (social
 * category) — like / heart toggle. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['like', 'button', 'heart', 'toggle'],
  description: 'LikeButton component — awaiting implementation.',
};
