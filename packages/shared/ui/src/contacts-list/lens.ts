/**
 * Lens manifest for the ContactsList component (social
 * category) — alphabetically indexed contact list. Tagged for
 * contacts / list / alphabetical / indexed lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['contacts', 'list', 'alphabetical', 'indexed'],
  description: 'ContactsList component — awaiting implementation.',
};
