import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['contacts', 'list', 'alphabetical', 'indexed'],
  description: 'ContactsList component — awaiting implementation.',
};
