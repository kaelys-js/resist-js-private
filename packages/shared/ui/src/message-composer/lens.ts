/**
 * Lens manifest for the MessageComposer component (social
 * category) — multi-line message input. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['message', 'composer', 'input', 'rich'],
  description: 'Rich message input with attachments.',
};
