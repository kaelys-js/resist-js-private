import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['message', 'composer', 'input', 'rich'],
  description: 'Rich message input with attachments.',
};
