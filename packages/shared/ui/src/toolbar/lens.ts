/**
 * Lens manifest for the Toolbar component (navigation
 * category) — grouped action / control bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['toolbar', 'actions', 'bar', 'controls'],
  description:
    'A container for grouping related actions and controls, typically with buttons, toggles, and separators.',
};
