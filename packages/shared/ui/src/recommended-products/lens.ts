import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['recommended', 'products', 'suggestions'],
  description: 'Product recommendation row.',
};
