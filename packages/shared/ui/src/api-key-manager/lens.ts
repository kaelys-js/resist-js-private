import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['api-key', 'manager', 'create', 'revoke'],
  description: 'ApiKeyManager component — awaiting implementation.',
};
