/**
 * Lens manifest for the ViolinPlot component (data-display
 * category) — kernel-density violin chart. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['violin', 'distribution', 'chart'],
  description: 'Distribution density violin chart.',
};
