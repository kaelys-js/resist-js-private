/**
 * Lens manifest for the BorderBeam component (animation category)
 * — rotating glowing-beam border decoration around a container.
 * Tagged for border / beam / glow / rotating lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['border', 'beam', 'glow', 'rotating'],
  description: 'BorderBeam component — awaiting implementation.',
};
