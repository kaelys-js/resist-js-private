import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['linear', 'gauge', 'horizontal', 'indicator'],
  description: 'LinearGauge component — awaiting implementation.',
};
