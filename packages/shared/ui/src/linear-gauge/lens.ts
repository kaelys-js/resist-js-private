/**
 * Lens manifest for the LinearGauge component (data-display
 * category) — horizontal progress gauge. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['linear', 'gauge', 'horizontal', 'indicator'],
  description: 'LinearGauge component — awaiting implementation.',
};
