import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['wavy', 'background', 'wave', 'svg-animation'],
  description: 'WavyBackground component — awaiting implementation.',
};
