/**
 * Lens manifest for the SymptomChecker component (healthcare
 * category) — guided symptom selection flow. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['symptom', 'checker', 'diagnostic'],
  description: 'Symptom selection flow.',
};
