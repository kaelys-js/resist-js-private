import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['nav-link', 'sidebar-item', 'active-link', 'route'],
  description: 'NavLink component — awaiting implementation.',
};
