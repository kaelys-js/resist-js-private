/**
 * Lens manifest for the PodcastPlayer component (media
 * category) — podcast audio player. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['podcast', 'player', 'episodes', 'chapters'],
  description: 'PodcastPlayer component — awaiting implementation.',
};
