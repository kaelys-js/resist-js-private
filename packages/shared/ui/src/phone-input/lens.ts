/**
 * Lens manifest for the PhoneInput component (form
 * category) — international phone input. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['phone', 'input', 'international', 'country-code'],
  description: 'PhoneInput component — awaiting implementation.',
};
