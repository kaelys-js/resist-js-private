/**
 * Lens manifest for the FeedbackWidget component (marketing
 * category) — floating feedback / support form. Tagged for
 * feedback / widget / floating / form lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['feedback', 'widget', 'floating', 'form'],
  description: 'FeedbackWidget component — awaiting implementation.',
};
