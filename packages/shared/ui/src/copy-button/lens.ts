/**
 * Lens manifest for the CopyButton component (feedback
 * category) — copy-to-clipboard button with tooltip feedback and
 * accessible status. Tagged for clipboard / copy / feedback /
 * button lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['clipboard', 'copy', 'feedback', 'button'],
  description: 'Copy-to-clipboard button with tooltip feedback and accessible status.',
};
