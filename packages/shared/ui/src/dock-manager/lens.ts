/**
 * Lens manifest for the DockManager component (desktop
 * category) — IDE-style resizable docked panels manager. Tagged
 * for dock / manager / panels / resizable lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['dock', 'manager', 'panels', 'resizable'],
  description: 'DockManager component — awaiting implementation.',
};
