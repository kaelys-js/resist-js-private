import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['color', 'palette', 'generator', 'tool'],
  description: 'ColorPaletteGenerator component — awaiting implementation.',
};
