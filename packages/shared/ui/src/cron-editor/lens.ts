/**
 * Lens manifest for the CronEditor component (form category) —
 * visual cron-expression builder. Tagged for cron / schedule /
 * editor lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['cron', 'schedule', 'editor'],
  description: 'Visual cron expression builder.',
};
