import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['org-chart', 'hierarchy', 'tree', 'organizational'],
  description: 'A hierarchical visualization of organizational structure with interactive nodes.',
};
