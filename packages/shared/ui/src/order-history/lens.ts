/**
 * Lens manifest for the OrderHistory component (commerce
 * category) — past order list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['order', 'history', 'list'],
  description: 'Past orders list.',
};
