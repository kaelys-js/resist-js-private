/**
 * Lens manifest for the OnboardingChecklist component
 * (marketing category) — interactive setup checklist. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['onboarding', 'checklist', 'setup'],
  description: 'Setup completion checklist.',
};
