/**
 * Lens manifest for the LinkPreview component (overlay
 * category) — hover preview card for external links. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['link-preview', 'hover-card', 'url-preview', 'popover'],
  description: 'LinkPreview component — awaiting implementation.',
};
