/**
 * Lens manifest for the TextToSpeech component (utility
 * category) — speech-synthesis playback wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['tts', 'speech', 'synthesis'],
  description: 'Text-to-speech synthesis utility.',
};
