/**
 * Lens manifest for the DistanceCalculator component (maps
 * category) — distance-between-points calculator. Tagged for
 * distance / calculator / geo lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['distance', 'calculator', 'geo'],
  description: 'Distance between points calculator.',
};
