import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['transaction', 'list', 'debit-credit', 'history'],
  description: 'TransactionList component — awaiting implementation.',
};
