/**
 * Lens manifest for the ConditionalForm component (form
 * category) — form with conditional field visibility. Tagged
 * for conditional / form / visibility lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['conditional', 'form', 'visibility'],
  description: 'Form with conditional field visibility.',
};
