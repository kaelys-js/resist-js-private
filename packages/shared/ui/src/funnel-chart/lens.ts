/**
 * Lens manifest for the FunnelChart component (data-display
 * category) — conversion funnel with progressively narrowing
 * stages. Tagged for funnel / chart / conversion / stages
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['funnel', 'chart', 'conversion', 'stages'],
  description: 'FunnelChart component — awaiting implementation.',
};
