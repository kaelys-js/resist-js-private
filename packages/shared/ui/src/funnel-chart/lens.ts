import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['funnel', 'chart', 'conversion', 'stages'],
  description: 'FunnelChart component — awaiting implementation.',
};
