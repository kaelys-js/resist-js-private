/**
 * Lens manifest for the PrintDialog component (overlay
 * category) — print preview dialog. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['print', 'dialog', 'settings'],
  description: 'Print settings dialog.',
};
