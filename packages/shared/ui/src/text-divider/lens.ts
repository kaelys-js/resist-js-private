/**
 * Lens manifest for the TextDivider component (layout
 * category) — divider with centered text label. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['divider', 'text', 'separator', 'labeled'],
  description: 'A horizontal divider with text content (label, heading, or action) in the center.',
};
