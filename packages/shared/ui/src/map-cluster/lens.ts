/**
 * Lens manifest for the MapCluster component (maps category)
 * — clustered marker pins. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['map', 'cluster', 'grouped', 'markers'],
  description: 'MapCluster component — awaiting implementation.',
};
