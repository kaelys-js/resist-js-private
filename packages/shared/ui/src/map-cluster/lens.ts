import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'maps',
  tags: ['map', 'cluster', 'grouped', 'markers'],
  description: 'MapCluster component — awaiting implementation.',
};
