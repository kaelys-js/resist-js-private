import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['reward', 'card', 'prize', 'display'],
  description: 'RewardCard component — awaiting implementation.',
};
