/**
 * Lens manifest for the IncidentTimeline component (admin
 * category) — chronological incident update timeline. Tagged
 * for incident / timeline / updates / status lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['incident', 'timeline', 'updates', 'status'],
  description: 'IncidentTimeline component — awaiting implementation.',
};
