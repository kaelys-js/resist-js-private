import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['follow', 'button', 'toggle', 'subscribe'],
  description: 'FollowButton component — awaiting implementation.',
};
