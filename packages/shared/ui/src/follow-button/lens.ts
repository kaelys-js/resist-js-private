/**
 * Lens manifest for the FollowButton component (social
 * category) — toggleable follow / subscribe button. Tagged for
 * follow / button / toggle / subscribe lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['follow', 'button', 'toggle', 'subscribe'],
  description: 'FollowButton component — awaiting implementation.',
};
