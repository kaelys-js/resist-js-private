/**
 * Lens manifest for the BackgroundBeams component (animation
 * category) — animated light-ray background effect. Tagged for
 * beams / background / rays / light lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['beams', 'background', 'rays', 'light'],
  description: 'BackgroundBeams component — awaiting implementation.',
};
