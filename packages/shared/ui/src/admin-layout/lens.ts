/**
 * Lens manifest for the AdminLayout component (admin category)
 * — sidebar-plus-header dashboard layout for admin consoles.
 * Tagged for admin / layout / sidebar-header / dashboard lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['admin', 'layout', 'sidebar-header', 'dashboard'],
  description: 'AdminLayout component — awaiting implementation.',
};
