/**
 * Lens manifest for the SignaturePad component (form
 * category) — handwritten-signature drawing canvas. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['signature', 'drawing', 'canvas', 'handwriting'],
  description:
    'A drawing canvas for capturing handwritten signatures via mouse, touch, or stylus input.',
};
