/**
 * Lens manifest for the Swap component (animation category)
 * — animated dual-state toggle. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['swap', 'toggle', 'transition', 'flip'],
  description:
    'A component that toggles between two visual states (e.g., icons, text) with an animation.',
};
