/**
 * Lens manifest for the BottomNavigation component (mobile
 * category) — fixed bottom-of-screen tab navigation for mobile
 * interfaces. Tagged for navigation / bottom / mobile / tabs
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'mobile',
  tags: ['navigation', 'bottom', 'mobile', 'tabs'],
  description:
    'A fixed navigation bar at the bottom of the screen, commonly used in mobile interfaces.',
};
