/**
 * Lens manifest for the LoginForm component (form category)
 * — email / password sign-in form. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['login', 'form', 'authentication', 'credentials'],
  description: 'LoginForm component — awaiting implementation.',
};
