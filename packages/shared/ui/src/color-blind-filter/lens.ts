/**
 * Lens manifest for the ColorBlindFilter component (a11y
 * category) — color-vision filter toggle. Tagged for color-blind
 * / filter / a11y lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'a11y',
  tags: ['color-blind', 'filter', 'a11y'],
  description: 'Color vision filter toggle.',
};
