import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['radar', 'chart', 'spider', 'polygon'],
  description: 'RadarChart component — awaiting implementation.',
};
