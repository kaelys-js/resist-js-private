/**
 * Lens manifest for the Histogram component (data-display
 * category) — frequency distribution histogram. Tagged for
 * histogram / chart / distribution lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['histogram', 'chart', 'distribution'],
  description: 'Frequency distribution histogram.',
};
