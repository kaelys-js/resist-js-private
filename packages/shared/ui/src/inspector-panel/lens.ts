import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['inspector', 'panel', 'properties'],
  description: 'Collapsible properties inspector.',
};
