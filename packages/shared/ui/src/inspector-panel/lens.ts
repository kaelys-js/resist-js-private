/**
 * Lens manifest for the InspectorPanel component (desktop
 * category) — collapsible properties inspector. Tagged for
 * inspector / panel / properties lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'desktop',
  tags: ['inspector', 'panel', 'properties'],
  description: 'Collapsible properties inspector.',
};
