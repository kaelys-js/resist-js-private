/**
 * Lens manifest for the Stepper component (navigation
 * category) — multi-step wizard with content panels. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['stepper', 'wizard', 'steps', 'progress'],
  description:
    'A multi-step progress indicator showing the current position in a sequential workflow.',
};
