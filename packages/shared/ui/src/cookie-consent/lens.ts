/**
 * Lens manifest for the CookieConsent component (legal
 * category) — GDPR cookie-consent banner. Tagged for cookie /
 * consent / gdpr / privacy-banner lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'legal',
  tags: ['cookie', 'consent', 'gdpr', 'privacy-banner'],
  description: 'CookieConsent component — awaiting implementation.',
};
