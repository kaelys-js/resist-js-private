/**
 * Lens manifest for the BlobAnimation component (animation
 * category) — animated SVG blob shape that morphs between paths.
 * Tagged for blob / svg / morph lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['blob', 'svg', 'morph'],
  description: 'Animated SVG blob shape.',
};
