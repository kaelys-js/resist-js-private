import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['size', 'selector', 'variant', 'pills'],
  description: 'SizeSelector component — awaiting implementation.',
};
