import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['blockquote', 'quote', 'citation', 'text'],
  description: 'A styled quotation block for displaying cited text with optional attribution.',
};
