/**
 * Lens manifest for the DateRangeField component (date-time
 * category) — start / end date range input field. Tagged for
 * date / range / field / input lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['date', 'range', 'field', 'input'],
  description: 'DateRangeField component — awaiting implementation.',
};
