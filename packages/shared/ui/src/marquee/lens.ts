import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['marquee', 'scroll', 'ticker', 'continuous'],
  description:
    'An infinitely scrolling content strip, typically used for logos, testimonials, or announcements.',
};
