/**
 * Lens manifest for the FilterBar component (data-display
 * category) — composable filter chips bar. Tagged for filter /
 * bar / chips / composable lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['filter', 'bar', 'chips', 'composable'],
  description: 'FilterBar component — awaiting implementation.',
};
