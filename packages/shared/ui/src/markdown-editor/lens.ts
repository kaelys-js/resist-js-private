/**
 * Lens manifest for the MarkdownEditor component (form
 * category) — Markdown editor with live preview. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'content',
  tags: ['markdown', 'editor', 'preview', 'wysiwyg'],
  description: 'MarkdownEditor component — awaiting implementation.',
};
