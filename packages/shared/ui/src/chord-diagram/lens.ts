/**
 * Lens manifest for the ChordDiagram component (data-display
 * category) — circular flow / relationship diagram. Tagged for
 * chord / flow / diagram lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['chord', 'flow', 'diagram'],
  description: 'Circular flow relationship diagram.',
};
