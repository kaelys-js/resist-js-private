/**
 * Lens manifest for the WobbleCard component (animation
 * category) — hover-tilt wobble card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['wobble', 'card', 'tilt', 'hover-effect'],
  description: 'WobbleCard component — awaiting implementation.',
};
