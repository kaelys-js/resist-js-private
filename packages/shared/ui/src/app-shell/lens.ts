/**
 * Lens manifest for the AppShell component (layout category) —
 * sidebar-plus-header scaffold layout for full applications.
 * Tagged for app-shell / layout / sidebar-header / scaffold
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['app-shell', 'layout', 'sidebar-header', 'scaffold'],
  description: 'AppShell component — awaiting implementation.',
};
