/**
 * Lens manifest for the InventoryGrid component (gaming
 * category) — RPG inventory slot grid. Tagged for inventory /
 * grid / slot / game lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['inventory', 'grid', 'slot', 'game'],
  description: 'Item inventory slot grid.',
};
