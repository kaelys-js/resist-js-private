/**
 * Lens manifest for the QuotaBar component (admin category)
 * — quota usage progress bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['quota', 'limit', 'progress'],
  description: 'Quota limit progress bar.',
};
