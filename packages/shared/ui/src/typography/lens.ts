/**
 * Lens manifest for the Typography component (typography
 * category) — polymorphic text-style component. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['typography', 'text', 'heading', 'prose'],
  description:
    'A set of text components for headings, paragraphs, and inline text with consistent styling.',
};
