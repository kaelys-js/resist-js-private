import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['thumb', 'rating', 'up-down', 'feedback'],
  description: 'ThumbRating component — awaiting implementation.',
};
