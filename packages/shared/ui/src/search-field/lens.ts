/**
 * Lens manifest for the SearchField component (form
 * category) — accessible search input field. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['search', 'input', 'field', 'query'],
  description: 'SearchField component — awaiting implementation.',
};
