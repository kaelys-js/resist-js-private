/**
 * Lens manifest for the FocusTrap component (a11y category) —
 * keyboard focus containment for modals and dialogs. Tagged for
 * focus / trap / keyboard / containment lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'a11y',
  tags: ['focus', 'trap', 'keyboard', 'containment'],
  description: 'FocusTrap component — awaiting implementation.',
};
