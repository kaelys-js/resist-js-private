/**
 * Lens manifest for the NpsSurvey component (form category)
 * — NPS rating widget. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['nps', 'survey', 'score', 'promoter'],
  description: 'NpsSurvey component — awaiting implementation.',
};
