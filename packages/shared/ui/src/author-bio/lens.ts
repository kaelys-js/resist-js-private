/**
 * Lens manifest for the AuthorBio component (marketing category)
 * — author bio card with avatar, name, and short biography.
 * Tagged for author / bio / card / avatar lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['author', 'bio', 'card', 'avatar'],
  description: 'AuthorBio component — awaiting implementation.',
};
