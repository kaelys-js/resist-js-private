import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['vortex', 'background', 'spiral', 'particle'],
  description: 'VortexBackground component — awaiting implementation.',
};
