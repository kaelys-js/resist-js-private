/**
 * Lens manifest for the ParticleTrail component (animation
 * category) — cursor particle trail. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['particle', 'trail', 'mouse'],
  description: 'Mouse particle trail effect.',
};
