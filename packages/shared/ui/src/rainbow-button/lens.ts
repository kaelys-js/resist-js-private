import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['rainbow', 'button', 'gradient', 'animated'],
  description: 'RainbowButton component — awaiting implementation.',
};
