/**
 * Lens manifest for the Persona component (display category)
 * — user persona avatar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['persona', 'user', 'profile', 'avatar'],
  description: 'A composite display of a user avatar, name, and additional details.',
};
