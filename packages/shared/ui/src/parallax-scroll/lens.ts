import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['parallax', 'scroll', 'depth', 'motion'],
  description:
    'A container where nested elements move at different speeds during scroll, creating a depth illusion.',
};
