/**
 * Lens manifest for the NavigationRail component (navigation
 * category) — Material-style narrow nav rail. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['navigation-rail', 'vertical', 'icon-nav', 'material'],
  description: 'NavigationRail component — awaiting implementation.',
};
