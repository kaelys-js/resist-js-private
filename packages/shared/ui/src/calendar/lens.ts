/**
 * Lens manifest for the Calendar component (date-time category)
 * — interactive calendar for date selection with month/year
 * navigation. Tagged for calendar / date / picker / schedule
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['calendar', 'date', 'picker', 'schedule'],
  description: 'Interactive calendar for date selection with month/year navigation.',
};
