/**
 * Lens manifest for the AnimatedNumber component (animation
 * category) — animates between numeric values with counting /
 * rolling tween transitions. Tagged for animated / number /
 * counter / tween lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['animated', 'number', 'counter', 'tween'],
  description:
    'A component that animates between numeric values with counting/rolling transitions.',
};
