import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['tab-navigation', 'page-tabs', 'route-tabs', 'nav'],
  description: 'TabNavigation component — awaiting implementation.',
};
