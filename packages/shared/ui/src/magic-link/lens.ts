import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['magic-link', 'auth', 'passwordless'],
  description: 'Magic link authentication flow.',
};
