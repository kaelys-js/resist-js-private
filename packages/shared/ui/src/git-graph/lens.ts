/**
 * Lens manifest for the GitGraph component (devtools
 * category) — git commit graph visualisation. Tagged for git /
 * graph / commit lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['git', 'graph', 'commit'],
  description: 'Git commit graph visualization.',
};
