/**
 * Lens manifest for the CourseCard component (education
 * category) — course overview card. Tagged for course / card /
 * education lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'education',
  tags: ['course', 'card', 'education'],
  description: 'Course overview card.',
};
