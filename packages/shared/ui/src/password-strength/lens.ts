import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['password', 'strength', 'meter', 'indicator'],
  description: 'PasswordStrength component — awaiting implementation.',
};
