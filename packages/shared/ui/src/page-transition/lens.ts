/**
 * Lens manifest for the PageTransition component (animation
 * category) — page navigation transition wrapper. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['page', 'transition', 'route'],
  description: 'Page-level route transition.',
};
