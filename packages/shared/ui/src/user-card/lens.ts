import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'display',
  tags: ['user', 'card', 'profile', 'persona'],
  description: 'UserCard component — awaiting implementation.',
};
