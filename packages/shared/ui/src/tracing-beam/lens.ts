/**
 * Lens manifest for the TracingBeam component (animation
 * category) — scroll-tracing SVG path. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['tracing', 'beam', 'scroll', 'progress'],
  description:
    'An animated SVG path that traces along the page as the user scrolls, highlighting the current reading position.',
};
