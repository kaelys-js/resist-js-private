import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['input', 'number', 'stepper', 'increment'],
  description: 'A numeric input with increment/decrement buttons and optional formatting.',
};
