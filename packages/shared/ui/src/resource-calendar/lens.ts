import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['resource', 'calendar', 'timeline'],
  description: 'Resource/room timeline calendar.',
};
