import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['toggle', 'button', 'pressed', 'switch', 'tv-variant'],
  description: 'Two-state button that can be toggled on or off.',
};
