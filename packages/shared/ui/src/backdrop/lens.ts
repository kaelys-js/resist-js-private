/**
 * Lens manifest for the Backdrop component (overlay category) —
 * dimming scrim layer placed behind modals and popovers. Tagged
 * for backdrop / overlay / scrim / dim lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['backdrop', 'overlay', 'scrim', 'dim'],
  description: 'Backdrop component — awaiting implementation.',
};
