/**
 * Lens manifest for the DemoRequestForm component (marketing
 * category) — sales demo booking form. Tagged for demo /
 * request / form / booking lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['demo', 'request', 'form', 'booking'],
  description: 'DemoRequestForm component — awaiting implementation.',
};
