/**
 * Lens manifest for the SaveSlot component (gaming
 * category) — save-game slot card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['save', 'slot', 'game'],
  description: 'Save game slot card.',
};
