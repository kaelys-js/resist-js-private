/**
 * Lens manifest for the ThreadView component (social
 * category) — threaded message conversation view. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['thread', 'view', 'message'],
  description: 'Threaded message view.',
};
