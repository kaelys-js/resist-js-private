/**
 * `@/ui` test-results — lens registry metadata for the TestResults component.
 *
 * @module
 */
import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'devtools',
  tags: ['test', 'results', 'suite'],
  description: 'Test suite results display.',
};
