/**
 * Lens manifest for the MagicCard component (animation
 * category) — cursor-following gradient card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['magic', 'card', 'spotlight', 'gradient-follow'],
  description: 'MagicCard component — awaiting implementation.',
};
