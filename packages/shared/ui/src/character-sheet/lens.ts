/**
 * Lens manifest for the CharacterSheet component (gaming
 * category) — RPG character stats / attributes sheet. Tagged
 * for character / sheet / stats / rpg lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'gaming',
  tags: ['character', 'sheet', 'stats', 'rpg'],
  description: 'Character stats/attributes sheet.',
};
