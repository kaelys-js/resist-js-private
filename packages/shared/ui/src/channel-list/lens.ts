/**
 * Lens manifest for the ChannelList component (social category)
 * — chat channel / room list sidebar. Tagged for channel / list
 * / chat / room lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['channel', 'list', 'chat', 'room'],
  description: 'Chat channel/room list.',
};
