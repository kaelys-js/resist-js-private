import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'lens',
  tags: ['copy', 'import', 'clipboard', 'code'],
  description: 'One-click copy-to-clipboard import path badge.',
};
