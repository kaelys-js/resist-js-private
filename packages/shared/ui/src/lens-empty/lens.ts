/**
 * Lens manifest for the LensEmpty infrastructure component —
 * empty preview card placeholder. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'lens',
  tags: ['empty-state', 'placeholder', 'documentation'],
  description: 'Empty state placeholder for documentation sections.',
};
