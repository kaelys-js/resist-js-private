import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['scroll', 'trigger', 'intersection', 'animate'],
  description: 'ScrollTrigger component — awaiting implementation.',
};
