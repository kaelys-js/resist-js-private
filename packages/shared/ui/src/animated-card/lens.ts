import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['animated', 'card', 'hover', 'motion'],
  description:
    'A card component with hover effects, 3D transforms, spotlight effects, or animated borders.',
};
