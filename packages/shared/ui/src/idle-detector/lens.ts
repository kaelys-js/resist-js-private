/**
 * Lens manifest for the IdleDetector component (utility
 * category) — user idle / away detection. Tagged for idle /
 * detector / inactivity lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'utility',
  tags: ['idle', 'detector', 'inactivity'],
  description: 'User idle/away detection.',
};
