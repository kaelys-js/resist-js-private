/**
 * Lens manifest for the MultiSlider component (form
 * category) — multi-thumb slider. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['multi', 'slider', 'handles', 'track'],
  description: 'MultiSlider component — awaiting implementation.',
};
