/**
 * Lens manifest for the Waveform component (media category)
 * — audio peak waveform visualisation. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['waveform', 'audio', 'visualization', 'peaks'],
  description: 'Waveform component — awaiting implementation.',
};
