/**
 * Lens manifest for the FinanceCard component (finance
 * category) — financial summary card with value and trend
 * indicator. Tagged for finance / metric / card / dashboard
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['finance', 'metric', 'card', 'dashboard'],
  description: 'Financial summary card with value and trend indicator.',
};
