/**
 * Lens manifest for the BudgetTracker component (finance
 * category) — budget-vs-actual tracker visualization. Tagged for
 * budget / tracker / finance lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['budget', 'tracker', 'finance'],
  description: 'Budget vs actual tracker.',
};
