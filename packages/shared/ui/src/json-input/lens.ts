/**
 * Lens manifest for the JsonInput component (form category)
 * — structured JSON value editor. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['json', 'input', 'structured-data', 'editor'],
  description: 'JsonInput component — awaiting implementation.',
};
