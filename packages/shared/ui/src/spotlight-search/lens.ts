/**
 * Lens manifest for the SpotlightSearch component (overlay
 * category) — full-screen Spotlight search. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['spotlight', 'search', 'overlay'],
  description: 'Full-screen spotlight search overlay.',
};
