/**
 * Lens manifest for the ImageEditor component (media
 * category) — image editor with crop / rotate / filter
 * operations. Tagged for image / editor / crop-rotate / filter
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['image', 'editor', 'crop-rotate', 'filter'],
  description: 'ImageEditor component — awaiting implementation.',
};
