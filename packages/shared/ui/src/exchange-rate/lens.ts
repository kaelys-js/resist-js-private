/**
 * Lens manifest for the ExchangeRate component (finance
 * category) — currency exchange-rate display. Tagged for
 * exchange / rate / currency lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['exchange', 'rate', 'currency'],
  description: 'Currency exchange rate display.',
};
