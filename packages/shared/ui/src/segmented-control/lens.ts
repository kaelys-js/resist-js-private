/**
 * Lens manifest for the SegmentedControl component (form
 * category) — iOS-style segmented option control. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['segmented', 'control', 'tabs', 'selection'],
  description:
    'A set of exclusive options displayed as connected buttons, similar to iOS-style segmented controls.',
};
