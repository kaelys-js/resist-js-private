/**
 * Lens manifest for the Menu component (overlay category)
 * — generic command-style menu list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['menu', 'list', 'actions', 'context'],
  description: 'A vertical navigation list with items, groups, and nested submenus.',
};
