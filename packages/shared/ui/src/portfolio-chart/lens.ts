/**
 * Lens manifest for the PortfolioChart component (finance
 * category) — portfolio performance chart. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'finance',
  tags: ['portfolio', 'chart', 'investment'],
  description: 'Investment portfolio chart.',
};
