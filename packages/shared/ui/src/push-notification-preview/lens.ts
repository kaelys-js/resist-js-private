/**
 * Lens manifest for the PushNotificationPreview component
 * (overlay category) — push notification preview card.
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['push', 'notification', 'preview'],
  description: 'Push notification preview card.',
};
