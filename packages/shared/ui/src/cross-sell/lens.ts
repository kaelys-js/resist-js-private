/**
 * Lens manifest for the CrossSell component (commerce category)
 * — cross-sell / related-products suggestions. Tagged for
 * cross-sell / products / related lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['cross-sell', 'products', 'related'],
  description: 'Cross-sell product suggestions.',
};
