/**
 * Lens manifest for the MedicalTimeline component (healthcare
 * category) — patient medical event timeline. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'healthcare',
  tags: ['medical', 'timeline', 'history'],
  description: 'Medical history timeline.',
};
