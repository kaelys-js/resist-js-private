/**
 * Lens manifest for the Group component (layout category) —
 * inline-flex horizontal container. Tagged for group /
 * inline-flex / horizontal / spacing lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['group', 'inline-flex', 'horizontal', 'spacing'],
  description: 'Group component — awaiting implementation.',
};
