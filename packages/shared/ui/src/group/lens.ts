import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['group', 'inline-flex', 'horizontal', 'spacing'],
  description: 'Group component — awaiting implementation.',
};
