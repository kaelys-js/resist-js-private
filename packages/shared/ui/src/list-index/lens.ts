/**
 * Lens manifest for the ListIndex component (navigation
 * category) — sticky alphabetical list index. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['list', 'index', 'alphabetical'],
  description: 'Alphabetical sidebar index.',
};
