/**
 * Lens manifest for the ContributionGraph component (data-
 * display category) — activity heat-map grid (GitHub-style).
 * Tagged for contribution / heatmap / activity lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['contribution', 'heatmap', 'activity'],
  description: 'Activity heatmap grid.',
};
