/**
 * Lens manifest for the TabMenu component (navigation
 * category) — horizontal tab-bar nav. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['tab-menu', 'horizontal-nav', 'menu-tabs', 'tab-bar'],
  description: 'TabMenu component — awaiting implementation.',
};
