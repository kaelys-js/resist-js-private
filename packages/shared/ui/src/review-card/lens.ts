/**
 * Lens manifest for the ReviewCard component (marketing
 * category) — single user review card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'marketing',
  tags: ['review', 'card', 'rating'],
  description: 'User review with rating.',
};
