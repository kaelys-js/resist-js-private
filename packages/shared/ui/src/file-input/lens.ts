/**
 * Lens manifest for the FileInput component (form category) —
 * styled file selection input with browse trigger. Tagged for
 * file / input / upload / browse lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['file', 'input', 'upload', 'browse'],
  description: 'FileInput component — awaiting implementation.',
};
