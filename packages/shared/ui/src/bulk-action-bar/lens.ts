/**
 * Lens manifest for the BulkActionBar component (data-display
 * category) — toolbar that surfaces bulk actions over a
 * multi-select selection. Tagged for bulk / action / multi-select
 * / toolbar lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['bulk', 'action', 'multi-select', 'toolbar'],
  description: 'BulkActionBar component — awaiting implementation.',
};
