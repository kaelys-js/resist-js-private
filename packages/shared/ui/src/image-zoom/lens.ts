/**
 * Lens manifest for the ImageZoom component (media category)
 * — magnify-on-hover loupe. Tagged for image / zoom / magnify /
 * loupe lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['image', 'zoom', 'magnify', 'loupe'],
  description: 'ImageZoom component — awaiting implementation.',
};
