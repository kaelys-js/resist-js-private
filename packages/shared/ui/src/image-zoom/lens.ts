import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['image', 'zoom', 'magnify', 'loupe'],
  description: 'ImageZoom component — awaiting implementation.',
};
