/**
 * Lens manifest for the BackgroundGradient component (animation
 * category) — animated mesh-gradient background. Tagged for
 * gradient / background / mesh / animated lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['gradient', 'background', 'mesh', 'animated'],
  description: 'BackgroundGradient component — awaiting implementation.',
};
