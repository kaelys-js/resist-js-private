/**
 * Lens manifest for the VideoThumbnail component (media
 * category) — clickable video preview thumbnail. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['video', 'thumbnail', 'preview'],
  description: 'Video preview thumbnail with play icon.',
};
