/**
 * Lens manifest for the StepProgress component (feedback
 * category) — horizontal step progress bar. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['step', 'progress', 'indicator'],
  description: 'Horizontal step progress indicator.',
};
