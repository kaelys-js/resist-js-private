/**
 * Lens manifest for the ZStack component (layout category)
 * — overlay stacking container. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['z-stack', 'overlay', 'stacking'],
  description: 'Overlay stacking container.',
};
