/**
 * Lens manifest for the BackgroundBoxes component (animation
 * category) — interactive grid-of-boxes background with hover
 * effects. Tagged for boxes / background / grid / hover-effect
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['boxes', 'background', 'grid', 'hover-effect'],
  description: 'BackgroundBoxes component — awaiting implementation.',
};
