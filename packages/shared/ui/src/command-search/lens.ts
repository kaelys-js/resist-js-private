import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'overlay',
  tags: ['search', 'command', 'keyboard', 'global', 'compound'],
  description: 'Global command search dialog with keyboard shortcut and grouped results.',
};
