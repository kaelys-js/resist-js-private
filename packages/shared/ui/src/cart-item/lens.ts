/**
 * Lens manifest for the CartItem component (commerce category)
 * — single line-item row in a shopping cart with quantity
 * controls. Tagged for cart / item / line-item / quantity
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['cart', 'item', 'line-item', 'quantity'],
  description: 'CartItem component — awaiting implementation.',
};
