/**
 * Lens manifest for the RangeCalendar component (date-time
 * category) — date-range start/end calendar picker. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'date-time',
  tags: ['calendar', 'range', 'date', 'selection'],
  description: 'Calendar for selecting a date range with start and end dates.',
};
