import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'a11y',
  tags: ['focus', 'ring', 'indicator', 'a11y'],
  description: 'Custom focus ring indicator.',
};
