/**
 * Lens manifest for the ImpersonationBanner component (admin
 * category) — warning bar for user impersonation. Tagged for
 * impersonation / banner / warning / user lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['impersonation', 'banner', 'warning', 'user'],
  description: 'ImpersonationBanner component — awaiting implementation.',
};
