import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['responsive', 'grid', 'breakpoint'],
  description: 'Breakpoint-aware responsive grid.',
};
