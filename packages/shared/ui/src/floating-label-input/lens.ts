/**
 * Lens manifest for the FloatingLabelInput component (form
 * category) — Material-style text input with animated label.
 * Tagged for input / floating-label / material / animated
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['input', 'floating-label', 'material', 'animated'],
  description: 'FloatingLabelInput component — awaiting implementation.',
};
