import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['input', 'floating-label', 'material', 'animated'],
  description: 'FloatingLabelInput component — awaiting implementation.',
};
