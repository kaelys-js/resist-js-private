/**
 * Lens manifest for the MediaRecorder component (media
 * category) — record audio / video. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['media', 'recorder', 'audio-video', 'capture'],
  description: 'MediaRecorder component — awaiting implementation.',
};
