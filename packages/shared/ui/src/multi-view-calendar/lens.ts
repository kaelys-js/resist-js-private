import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['multi-view', 'calendar', 'side-by-side', 'months'],
  description: 'MultiViewCalendar component — awaiting implementation.',
};
