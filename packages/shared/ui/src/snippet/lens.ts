import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'typography',
  tags: ['code', 'snippet', 'copy', 'inline'],
  description: 'A styled container for displaying short code or text snippets with a copy button.',
};
