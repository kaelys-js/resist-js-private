/**
 * Lens manifest for the AvatarEditor component (form category)
 * — avatar upload + crop editor for profile photos. Tagged for
 * avatar / editor / crop / upload lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['avatar', 'editor', 'crop', 'upload'],
  description: 'AvatarEditor component — awaiting implementation.',
};
