import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'feedback',
  tags: ['snackbar', 'toast', 'android', 'bottom-notification'],
  description: 'Snackbar component — awaiting implementation.',
};
