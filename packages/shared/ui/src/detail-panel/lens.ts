/**
 * Lens manifest for the DetailPanel component (data-display
 * category) — slide-out record detail view. Tagged for detail
 * / panel / slide-out lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['detail', 'panel', 'slide-out'],
  description: 'Slide-out record detail view.',
};
