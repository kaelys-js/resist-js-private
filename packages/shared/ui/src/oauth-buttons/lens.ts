/**
 * Lens manifest for the OauthButtons component (form
 * category) — OAuth provider button row. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['oauth', 'social', 'login', 'buttons'],
  description: 'Row of OAuth provider buttons.',
};
