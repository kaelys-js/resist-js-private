/**
 * Lens manifest for the MultiStepForm component (form
 * category) — wizard-style multi-step form. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['multi-step', 'wizard', 'form'],
  description: 'Multi-page form with step tracking.',
};
