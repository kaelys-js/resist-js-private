/**
 * Lens manifest for the ColorField component (form category) —
 * text input for hex / CSS color values. Tagged for color /
 * input / hex / picker lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'form',
  tags: ['color', 'input', 'hex', 'picker'],
  description: 'ColorField component — awaiting implementation.',
};
