import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['gallery', 'image', 'lightbox', 'carousel'],
  description:
    'An advanced image gallery with thumbnails, fullscreen mode, indicators, and autoplay.',
};
