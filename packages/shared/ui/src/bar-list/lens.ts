import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['bar-list', 'horizontal-bars', 'ranked', 'comparison'],
  description: 'BarList component — awaiting implementation.',
};
