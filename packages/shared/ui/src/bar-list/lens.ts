/**
 * Lens manifest for the BarList component (data-display category)
 * — ranked horizontal-bar list for comparison views. Tagged for
 * bar-list / horizontal-bars / ranked / comparison lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['bar-list', 'horizontal-bars', 'ranked', 'comparison'],
  description: 'BarList component — awaiting implementation.',
};
