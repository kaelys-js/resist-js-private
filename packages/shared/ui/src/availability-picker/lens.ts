/**
 * Lens manifest for the AvailabilityPicker component (scheduling
 * category) — selects available time slots from a schedule.
 * Tagged for availability / picker / time-slots / schedule
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'scheduling',
  tags: ['availability', 'picker', 'time-slots', 'schedule'],
  description: 'AvailabilityPicker component — awaiting implementation.',
};
