import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'data-display',
  tags: ['timer', 'countdown', 'elapsed', 'stopwatch'],
  description: 'TimerDisplay component — awaiting implementation.',
};
