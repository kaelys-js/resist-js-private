/**
 * Lens manifest for the Container component (layout category) —
 * max-width responsive content container. Tagged for container
 * / max-width / responsive / wrapper lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'layout',
  tags: ['container', 'max-width', 'responsive', 'wrapper'],
  description: 'Container component — awaiting implementation.',
};
