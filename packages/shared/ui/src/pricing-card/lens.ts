/**
 * Lens manifest for the PricingCard component (marketing
 * category) — pricing tier card. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'commerce',
  tags: ['pricing', 'plan', 'card'],
  description: 'Individual pricing plan card.',
};
