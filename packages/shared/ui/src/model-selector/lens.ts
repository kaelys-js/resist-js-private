/**
 * Lens manifest for the ModelSelector component (ai
 * category) — AI model picker. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['model', 'selector', 'ai', 'dropdown'],
  description: 'ModelSelector component — awaiting implementation.',
};
