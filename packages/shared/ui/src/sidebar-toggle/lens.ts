/**
 * Lens manifest for the SidebarToggle component (navigation
 * category) — sidebar visibility toggle button. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'navigation',
  tags: ['sidebar', 'toggle', 'menu', 'collapse'],
  description: 'Button to toggle sidebar visibility.',
};
