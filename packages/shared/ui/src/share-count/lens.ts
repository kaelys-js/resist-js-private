/**
 * Lens manifest for the ShareCount component (social
 * category) — social share count badge. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['share', 'count', 'social'],
  description: 'Social share count display.',
};
