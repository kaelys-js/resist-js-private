/**
 * Lens manifest for the TextHoverEffect component (animation
 * category) — cursor-following text distortion. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'animation',
  tags: ['text', 'hover', 'distortion', 'interactive'],
  description: 'TextHoverEffect component — awaiting implementation.',
};
