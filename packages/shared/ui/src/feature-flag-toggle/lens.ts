/**
 * Lens manifest for the FeatureFlagToggle component (admin
 * category) — admin on/off switch for runtime feature flags.
 * Tagged for feature-flag / toggle / on-off / controls lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'admin',
  tags: ['feature-flag', 'toggle', 'on-off', 'controls'],
  description: 'FeatureFlagToggle component — awaiting implementation.',
};
