/**
 * Lens manifest for the EqualizerDisplay component (media
 * category) — animated audio level / frequency bar
 * visualization. Tagged for equalizer / frequency / bars / audio
 * lookups.
 *
 * @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'media',
  tags: ['equalizer', 'frequency', 'bars', 'audio'],
  description: 'EqualizerDisplay component — awaiting implementation.',
};
