/**
 * Lens manifest for the SocialLogin component (social
 * category) — connected social-account list. @module
 */

import type { LensMeta } from '../lens/types.js';

export const meta: LensMeta = {
  category: 'social',
  tags: ['social', 'login', 'accounts'],
  description: 'Social account connection list.',
};
