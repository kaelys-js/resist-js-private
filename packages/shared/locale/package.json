{
  "name": "@/locale",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project locale",
    "qa:test:unit": "pnpm -w exec vitest run --project locale",
    "qa:test:coverage": "pnpm -w exec vitest run --project locale --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project locale",
    "qa:type-check": "tsgo --noEmit"
  },
  "dependencies": {
    "@/schemas/common": "workspace:*",
    "@/schemas/result": "workspace:*",
    "@/utils/result": "workspace:*"
  },
  "devDependencies": {
    "@/test-presets": "workspace:*"
  }
}
