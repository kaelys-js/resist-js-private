{
  "name": "@/utils/result",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project utils-result",
    "qa:test:unit": "pnpm -w exec vitest run --project utils-result",
    "qa:test:coverage": "pnpm -w exec vitest run --project utils-result --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project utils-result",
    "qa:type-check": "tsgo --noEmit"
  },
  "dependencies": {
    "@/schemas/common": "workspace:*",
    "@/schemas/result": "workspace:*"
  },
  "devDependencies": {
    "@/test-presets": "workspace:*"
  }
}
