{
  "name": "@/utils/web-vitals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project utils-web-vitals",
    "qa:test:unit": "pnpm -w exec vitest run --project utils-web-vitals",
    "qa:test:coverage": "pnpm -w exec vitest run --project utils-web-vitals --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project utils-web-vitals"
  },
  "dependencies": {
    "perfume.js": "^9.4.0"
  }
}
