{
  "name": "@/utils/beacon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project utils-beacon",
    "qa:test:unit": "pnpm -w exec vitest run --project utils-beacon",
    "qa:test:coverage": "pnpm -w exec vitest run --project utils-beacon --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project utils-beacon",
    "qa:type-check": "tsgo --noEmit"
  },
  "dependencies": {
    "@/schemas/common": "workspace:*",
    "@/schemas/result": "workspace:*",
    "@/utils/core": "workspace:*",
    "@/utils/result": "workspace:*"
  },
  "devDependencies": {
    "@/test-presets": "workspace:*"
  }
}
