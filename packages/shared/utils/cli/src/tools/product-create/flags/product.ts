/**
 * Product-Create Tool — Product Flag
 *
 * Re-exports the shared `--product` / `-p` flag definition.
 * Uses `ProductNameSchema` — rejects invalid names at parse time.
 *
 * @module
 */

export { default } from '@/utils/flags/shared/product';
