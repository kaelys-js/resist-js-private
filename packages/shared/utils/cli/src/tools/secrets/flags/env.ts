/**
 * Secrets Tool — Env Flag
 *
 * Re-exports the shared `--env` flag definition.
 * Uses `StandardEnvironmentSchema` — rejects invalid environments at parse time.
 *
 * @module
 */

export { default } from '@/utils/flags/shared/env';
