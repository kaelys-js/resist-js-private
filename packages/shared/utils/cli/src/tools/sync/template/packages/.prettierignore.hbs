{{syncHeader "#" "https://prettier.io/docs/en/ignore"}}

# Prettier is only used for .svelte files (Biome handles everything else)
# The qa:format script already scopes prettier to '**/*.svelte'

{{> ignore-common}}
