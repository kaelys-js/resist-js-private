{
  "name": "@/utils/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project utils-core",
    "qa:test:unit": "pnpm -w exec vitest run --project utils-core",
    "qa:test:coverage": "pnpm -w exec vitest run --project utils-core --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project utils-core"
  }
}
