{
  "name": "@/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "vitest run",
    "qa:test:unit": "vitest run",
    "qa:test:coverage": "vitest run --coverage",
    "qa:benchmark": "vitest bench",
    "qa:type-check": "tsc --noEmit",
    "qa:checks": "pnpm --filter @/cli tool checks --cwd ."
  },
  "dependencies": {
    "@/schemas/common": "workspace:*",
    "@/schemas/core-config": "workspace:*",
    "@/schemas/result": "workspace:*",
    "@/utils/core": "workspace:*",
    "@/utils/result": "workspace:*",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "@/config/test": "workspace:*",
    "vitest": "^4.0.18"
  }
}
