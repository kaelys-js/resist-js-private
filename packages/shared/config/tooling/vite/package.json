{
  "name": "@/config/tooling/vite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist coverage .turbo",
    "qa:test": "pnpm -w exec vitest run --project config-tooling-vite",
    "qa:test:coverage": "pnpm -w exec vitest run --project config-tooling-vite --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project config-tooling-vite"
  },
  "exports": {
    ".": "./src/index.ts",
    "./template-html": "./src/vite-plugin-template-html.ts",
    "./lazy-plugin": "./src/vite-plugin-lazy.ts"
  }
}
