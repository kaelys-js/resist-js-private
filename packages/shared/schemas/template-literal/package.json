{
  "name": "@/schemas/template-literal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project schemas-template-literal",
    "qa:test:unit": "pnpm -w exec vitest run --project schemas-template-literal",
    "qa:test:coverage": "pnpm -w exec vitest run --project schemas-template-literal --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project schemas-template-literal",
    "qa:type-check": "tsgo --noEmit",
    "qa:checks": "pnpm --filter @/cli tool checks --cwd ."
  }
}
