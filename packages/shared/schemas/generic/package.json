{
  "name": "@/schemas/generic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project schemas-generic",
    "qa:test:unit": "pnpm -w exec vitest run --project schemas-generic",
    "qa:test:coverage": "pnpm -w exec vitest run --project schemas-generic --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project schemas-generic"
  }
}
