{
  "name": "@/schemas/common",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project schemas-common",
    "qa:test:unit": "pnpm -w exec vitest run --project schemas-common",
    "qa:test:coverage": "pnpm -w exec vitest run --project schemas-common --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project schemas-common"
  }
}
