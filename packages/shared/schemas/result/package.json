{
  "name": "@/schemas/result",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project schemas-result",
    "qa:test:unit": "pnpm -w exec vitest run --project schemas-result",
    "qa:test:coverage": "pnpm -w exec vitest run --project schemas-result --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project schemas-result",
    "qa:type-check": "tsgo --noEmit"
  },
  "devDependencies": {
    "@/test-presets": "workspace:*"
  }
}
