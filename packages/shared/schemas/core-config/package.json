{
  "name": "@/schemas/core-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "qa:test": "pnpm -w exec vitest run --project schemas-core-config",
    "qa:test:unit": "pnpm -w exec vitest run --project schemas-core-config",
    "qa:test:coverage": "pnpm -w exec vitest run --project schemas-core-config --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project schemas-core-config",
    "qa:checks": "pnpm --filter @/cli tool checks --cwd ."
  }
}
