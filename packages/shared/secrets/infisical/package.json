{
  "name": "@/secrets/infisical",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist coverage .turbo",
    "qa:test": "pnpm -w exec vitest run --project secrets-infisical",
    "qa:test:unit": "pnpm -w exec vitest run --project secrets-infisical",
    "qa:test:coverage": "pnpm -w exec vitest run --project secrets-infisical --coverage",
    "qa:benchmark": "pnpm -w exec vitest bench --project secrets-infisical"
  },
  "dependencies": {
    "@infisical/sdk": "^2.0.0"
  }
}
