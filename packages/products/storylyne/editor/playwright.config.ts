/**
 * Playwright E2E test configuration for the Storylyne editor.
 *
 * Uses the shared preset from `@/test-presets`. Override any defaults
 * by passing options to `createPlaywrightConfig()`.
 *
 * @module
 */

import { createPlaywrightConfig } from '@/test-presets/presets/playwright';

export default createPlaywrightConfig();
