/**
 * Data service barrel export.
 *
 * Finance data CRUD is handled by SvelteKit API routes reading/writing
 * JSON files. This module re-exports server-side data types.
 *
 * @module
 */

export type { ServerUser } from './types';
