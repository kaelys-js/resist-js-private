# @/cli/sync Phase 53 — Backport Workspace Config Additions to Sync Templates

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Date**: 2026-03-31
**Package**: `@/cli` (`packages/shared/utils/cli/src/tools/sync/`)
**Goal**: Sync templates are missing workspace-specific patterns that apply to any project using this stack. Backport 8 changes from workspace root files into the HBS templates so `sync` produces correct output.
**Architecture**: Template-only changes. No runtime code modified. Additions are either hardcoded (universal patterns) or behind existing Handlebars conditionals.

Each task is atomic: implement -> verify (QA + tests) -> update plan -> next.

---

## Status Legend

- `[ ]` — Not started
- `[x]` — Done (implemented + verified + tests passing)
- `[~]` — In progress

---

## Baseline (before any changes)

| Metric | Value |
|--------|-------|
| Template files | 67 .hbs files |
| Missing workspace patterns | 8 gaps identified |
| Template-only workspace files | 2 (.eslintignore, .prettierignore root) |

---

## TASK 1 — Update ignore-common.hbs: add .resist-lint-cache.json

**Status**: [x]

**Plan**:
- Add `.resist-lint-cache.json` to the "Linting & Formatting Caches" section in `_partials/ignore-common.hbs`

**Files**:
- Modify: `sync/template/packages/_partials/ignore-common.hbs`

**Verification**: File updated, no syntax errors

---

## TASK 2 — Update .gitignore.hbs: add Claude Code, worktrees, Playwright MCP

**Status**: [x]

**Plan**:
- Add `.claude/*` block with negations for `settings.json`, `hooks/`, `skills/`
- Add `.worktrees/`
- Add `.playwright-mcp`
- Add `.github/skills`

**Files**:
- Modify: `sync/template/packages/.gitignore.hbs`

**Verification**: File updated, negation patterns correct

---

## TASK 3 — Update .npmrc.hbs: add peer deps, valibot hoist, node-options

**Status**: [x]

**Plan**:
- Add `auto-install-peers = true` and `strict-peer-dependencies = false` to pnpm block
- Add `public-hoist-pattern[] = *valibot*` to hoisting block
- Add `node-options` with the register-aliases loader bootstrap

**Files**:
- Modify: `sync/template/packages/.npmrc.hbs`

**Verification**: File updated, pnpm conditional intact

---

## TASK 4 — Create .eslintignore.hbs

**Status**: [x]

**Plan**:
- Create new template `sync/template/packages/.eslintignore.hbs`
- Include sync header and the workspace's eslintignore content pattern

**Files**:
- Create: `sync/template/packages/.eslintignore.hbs`

**Verification**: File created, valid HBS

---

## TASK 5 — Create root .prettierignore.hbs

**Status**: [x]

**Plan**:
- Create new template `sync/template/packages/.prettierignore.hbs` (root-level, distinct from the format config one)
- Include sync header, comment about Biome handling, and standard excludes

**Files**:
- Create: `sync/template/packages/.prettierignore.hbs`

**Verification**: File created, valid HBS

---

## TASK 6 — Update package.json.hbs: add pnpm.onlyBuiltDependencies

**Status**: [x]

**Plan**:
- Add `pnpm` section with `onlyBuiltDependencies` array inside pnpm conditional

**Files**:
- Modify: `sync/template/packages/package.json.hbs`

**Verification**: Valid JSON structure when rendered

---

## TASK 7 — Update turbo.json.hbs: add PLAYWRIGHT_* passthrough

**Status**: [x]

**Plan**:
- Add `"PLAYWRIGHT_*"` to `globalPassThroughEnv` array

**Files**:
- Modify: `sync/template/packages/turbo.json.hbs`

**Verification**: Valid JSON structure

---

## TASK 8 — Update .prettierrc.hbs: add top-level plugins

**Status**: [x]

**Plan**:
- Add `"plugins": ["prettier-plugin-svelte"]` at top level

**Files**:
- Modify: `sync/template/packages/shared/utils/cli/tools/format/config/.prettierrc.hbs`

**Verification**: Valid JSON structure, Svelte override still present

---

## TASK 9 — Update biome.jsonc.hbs: add files.includes excludes

**Status**: [x]

**Plan**:
- Add `files.includes` array with standard build/cache excludes

**Files**:
- Modify: `sync/template/packages/shared/utils/cli/tools/format/config/biome.jsonc.hbs`

**Verification**: Valid JSONC structure

---

## TASK 10 — Register Rules + Config

**Status**: [x]

**Plan**:
- No rules to register — template-only changes
- Verify new .hbs files are picked up by sync tool's template discovery
- Verify existing templates still valid

**Verification**: Template discovery finds new files

---

## TASK 11 — Full QA + Coverage

**Status**: [x]

**Plan**:
- Run `pnpm qa:test`
- Run `pnpm qa:lint --tools`
- Run `pnpm qa:format:check`
- Verify no regressions

**Verification**: All commands green, no regressions

---

## TASK 12 — Final Verification + Commit

**Status**: [x]

**Plan**:
- Verify all 8 template changes are present
- Verify 2 new template files exist
- Verify no existing template files broken
- Verify all workspace additions are reflected in templates
- Commit with descriptive message

**Verification**:
- `ignore-common.hbs` has `.resist-lint-cache.json`
- `.gitignore.hbs` has `.claude/*`, `.worktrees/`, `.playwright-mcp`
- `.npmrc.hbs` has `auto-install-peers`, `node-options`
- `.eslintignore.hbs` exists
- Root `.prettierignore.hbs` exists
- `package.json.hbs` has `pnpm.onlyBuiltDependencies`
- `turbo.json.hbs` has `PLAYWRIGHT_*`
- `.prettierrc.hbs` has top-level `plugins`
- `biome.jsonc.hbs` has `files.includes`
- Commit clean

---

## Execution Order

| Task | Description | Depends On |
|------|-------------|------------|
| 1 | Update ignore-common.hbs | — |
| 2 | Update .gitignore.hbs | — |
| 3 | Update .npmrc.hbs | — |
| 4 | Create .eslintignore.hbs | — |
| 5 | Create root .prettierignore.hbs | — |
| 6 | Update package.json.hbs | — |
| 7 | Update turbo.json.hbs | — |
| 8 | Update .prettierrc.hbs | — |
| 9 | Update biome.jsonc.hbs | — |
| 10 | Register + Config verification | 1-9 |
| 11 | Full QA + Coverage | 10 |
| 12 | Final verification + commit | 11 |
